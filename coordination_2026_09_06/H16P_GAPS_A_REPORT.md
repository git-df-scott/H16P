# H16P Gap-A completeness report

Scope: the 38 files listed in `H16P_GAPS_A_prose.txt`, all under
`/Users/scottg/Claude_all/H16P/` (read-only). Baseline for "already known" is
`H16P_SUMMARY.md` §§3–6 (lines 630–1320). Everything in Part B is a fact,
claim, obstruction, seed, route, next-step, disagreement or error that is
**not** in that summary. Paths below are relative to the repo root.

---

# PART A — COVERAGE MANIFEST

| # | Path | Contents | Read |
|---|---|---|---|
| 1 | `ASTRA_FIRST_STRIKE.md` | Strike-1 report. Six-part campaign map; Stieltjes/ECT structure theorem; universal auxiliary-cusp neighbourhood exclusion; three weighted-lobe inequalities; seven "inconsistencies resolved"; stop condition C; five-field YES/NO report; deliverables. | READ |
| 2 | `ASTRA_SECOND_STRIKE.md` | Strike-2 report. Lobe region as global anchor chart; certified rational lobe box; exact reconstruction IVP and forcing-sign correction; proof of `τ1>5/11`, `κ>21636/19043`; the 6 anchor trials and the 4-crossing/failed-S2 Green shot; guarded ordinary-fold reduction; six-field report. | READ |
| 3 | `ASTRA_THIRD_STRIKE.md` | Strike-3 report. Exact threshold-crossing path `γ(r)=T(r,(1+r)/2,(3+r)/4)`; certified late-root rational point; table of 8 tuned kappa shots with numerical first Z maxima; 4 confluent and 4 reverse-tangency shots; the fixed-λ endpoint obstruction; stop G; nine-field report. | READ |
| 4 | `ASTRA_FOURTH_STRIKE.md` | Strike-4 report. Theorem N as three global comparisons; new ingredients (moment convexity, `v_a` conjugation, corner integral); compact certificate `δ=1/64`; explicit scope limitation of the zero-count consequence; handoff `-K0` correction; seven-field report. | READ |
| 5 | `controls/requirements.txt` | `numpy==2.5.2`(pinned 2.3.5), `scipy==1.17.0`, `matplotlib==3.10.8`. | READ |
| 6 | `council/notes_third_routes.md` | Post-Q4 council audit: six repairs to `FASTRA_COUNCIL_2026_09_04.md`; the concrete generic-Q4 endpoint family and its exact Bautin/infinity algebra; a 4-step bounded preflight; certificate spec; resonant-infinity backup and five rejected alternatives; ordinal certificate-burden table. | READ |
| 7 | `fold_surface_2026_09_05/theory_negative_review.md` | Negative-K / actual-m sheet review: hash-fixed event snapshots; a finite-topology gate proving exactly two finite equilibria and **no saddle** on `1/3≤c≤1, m≥37`; exact remote Hopf threshold `m_H(c)`; `c=5/11` pole shown to be a chart artifact; `c=241/250` infinity-direction collision; large-m center scaling, first integral, Melnikov integral and the exact `c=1/3` balance with the conditional limit law. | READ |
| 8 | `kkl/NEXT_BOUNDED_TEST.md` | The proposed `c>1` field `c=1001/1000, α=-196/5, K=32039/6250`; bounded 4-step protocol; the four remote controls (calls 203–206) all returning inward, no U bracket, conditional origin block never run; budget 206/4096. | READ |
| 9 | `kkl/notes_geometry.md` | Exact equilibrium/infinity gates: `x=-1` contains no equilibrium; the uniformly proved segment `7/10≤c≤3/4, α=-216/(11c-5)`; strict rational bounds on `s*`, trace, determinant, focus discriminant; `N` and `E` boundary polynomials; trace-zero boundary; `ds*/dc<0`; a 17-point rational mesh. | READ |
| 10 | `kkl/notes_new_gate_review.md` | Independent review of the local-unfolding and other-strata notes. Verdict pass. Original-coordinate reversing involution with a polynomial (non-floating) replay; proof that the `K=J=0` organizer has **two** centers, with the second located exactly; independent remote-Hopf sign `K_H`; local return factorization and the stable-remote cone; the `c=1001/1000` quartic sign chart. | READ |
| 11 | `kkl/notes_return_review.md` | The full first/second return-derivative machinery: moving-event projection, determinant (`det(F,w)`) formulation, log-radius residual conventions, fold Jacobian, **which side of a fold adds which pair**, six named controls with saved numbers, the cubic-focus asymptotics, and §10's exact order-two `K=0` weak-focus computation. §11 reviews the 202-evaluation ledger. | READ |
| 12 | `q4/data/astra_verification.txt` | Strike-1 replay log: interpreter/lib versions, exact determinant, `w` Taylor, `W4(0)`, three period residuals, 7 unit tests. | READ |
| 13 | `q4/data/fifth_exact_checks.txt` | Strike-5 replay log: `check_fifth_two_anchor.py` (`231/50312`, `3/616`, `27/12578`, `1105/462`), `q4_fifth_green_checks.py`, `check_fifth_loop_gate.py`. | READ |
| 14 | `q4/data/fourth_exact_checks.txt` | Strike-4 replay log: `check_N_kernel.py` residual, `q4_N_loop_checks.py` (compact margin `395/3784704`). | READ |
| 15 | `q4/data/second_verification.txt` | Strike-2 replay log, incl. the full `q4_lobe_anchors.py` JSON (6 anchor records with exact lobe areas and 65-sample original probes) and the full `q4_green_shoot.py` JSON (4 P-roots and 4 Z heights). | READ |
| 16 | `q4/data/third_verification.txt` | Strike-3 replay log; 15 unit tests; per-script SHA256 and CPU; the `script_sha256`/`source_sha256` metadata-assembly error and its correction. | READ |
| 17 | `q4/notes_N_compact.md` | Strike-4: the global anchor envelope replacing compact certification. (C1)–(C6), incl. the **uniform** compact margin formula. Ends with the consequence audit that exhibits the allowed outside-lobe four-crossing sign pattern. | READ |
| 18 | `q4/notes_N_double.md` | Strike-4 kernel lane: exact conjugation `L_a`, `v_1` closed form, positive residual, positive causal Green comparison (K), corner beta identity (B), singular-endpoint integrability bookkeeping. | READ |
| 19 | `q4/notes_N_loop.md` | Strike-4 loop lane: two corrections to the proposed decomposition; strict moment convexity (N-L1)–(N-L2); center functional (N-L3)–(N-L4); cardinal signs (N-L5)–(N-L6); global corner comparison (N-L7); final chain (N-L8)–(N-L11). | READ |
| 20 | `q4/notes_audit_third.md` | Strike-3 independent Green audit: exact first-root threshold arithmetic; exact moment form of the first Green maximum; the four reverse-tangency lines with their stationary/pole table; audit of the late-root path; the closure of the joint endpoint matching constant `(G6)`; final hostile consistency review with three reported documentation defects. | READ |
| 21 | `q4/notes_certificate_second.md` | The exact rational weighted-lobe certificate: frozen point, four enclosures, tail majorant, why four signs prove all three lobe inequalities, `H(1)` closed functional and its `J_n(1)` derivation, the `10^-7` box, and a final hostile audit including the `Y`-endpoint logarithm asymptotics. | READ |
| 22 | `q4/notes_fifth_green.md` | Strike-5 Green analysis on the two-root fibre: the determinant `K(t)=P_B Z_C - Z_B P_C`, its one-way upward-crossing property, the **proved** exclusion when `P_B(r)≤0`, the reduction of the residual case to the single endpoint sign `K(r)`, `θ_e`, `θ'<0`, and the two extra necessary conditions `Z_B(r)>0`, `Z_V(r)<0`. | READ |
| 23 | `q4/notes_fifth_original_height.md` | Strike-5: the exact original-height functional `F=X-(j/R)Z`, its positive kernel `K_t(s)`, `F=X` at every `Y` zero, the `Φ`-root interlacing and (O6), and the proof that kernel positivity alone **cannot** close the fibre. | READ |
| 24 | `q4/notes_fifth_sign_chain.md` | Strike-5: the complete two-simple-root shooting classification (T1)(T2)(T3), the anchored sign-change lemma, endpoint behaviour with/without `H(1)=0` and `P_e=0`, non-generic cases, complete exclusion of the `β1=0` sector, and the necessary condition (T4) `σΦ(τ1)>0`. | READ |
| 25 | `q4/notes_fifth_two_anchor.md` | Strike-5 geometry: the two-anchor affine chart, the strictly decreasing residual ratio `R=B/V`, the **complete 7-case fibre classification table**, strengthened center-functional bounds (T6)(T7)(T8), `0<λ_c<η_B` (T9), `P_C<0,Z_C<0` (T10), mixed center sign (T11), the surviving branch (T12), and baseline anchor monotonicity (T13)(T14). | READ |
| 26 | `q4/notes_geometry.md` | Strike-1 zero geometry: original projective rank conditions, the maximal-contact uniqueness lemma, two impossible and one feasible birth configuration, the six-alternating-sign certificate shortcut, the auxiliary quadric `X0X3=X1X2`, all four Wronskian signs, exact fold/cusp parametrization, the cusp-neighbourhood exclusion theorem, and the weighted-lobe test in lobe-area form. | READ |
| 27 | `q4/notes_green_third.md` | Strike-3 delayed-anchor Green analysis: the `μ_*` coefficient limits at the corner, `(G1)`, the two exact `a=1` cancellations `(G2)(G3)`, the **finite-kappa endpoint-balance table at k=8,9,16**, the scaled-forcing sign lemma `(G4)`, the matched limit `(G5)` and the uniform bound `(G6)`. | READ |
| 28 | `q4/notes_lobe_second.md` | Strike-2 lobe source notes: global anchor diffeomorphism theorem with explicit matrix `E(y)` and formula (A); the exact interlacing `x1<y1<x2<y2<x3<y3`; the certified point/box; boundedness proof and the box `7/6<A<85/31` etc.; four boundary mechanisms and their exhaustiveness proof; spines and the deformation-retraction caveat; structural `Y0<0`; box exclusion. | READ |
| 29 | `q4/notes_pf.md` | Picard–Fuchs lane: coefficient-map determinant; universal period reduction; center and loop expansions; the derived positive Stieltjes representation with explicit density and Abel/Wronskian proof; the `b_*(t)` inflection theorem with a covariance proof; the ECT statement; the cusp exclusion; and §7 on what the PF reduction does **not** give about a common scalar operator. | READ |
| 30 | `q4/notes_threshold_third.md` | Strike-3 threshold notes: transversal crossing at `r=5/11`; closed moment recurrences for `J_n`, `K_j`, and `J_n(1)`; the certified late-root point and `10^-8` box; the full matched endpoint asymptotics with exact limits for `e,V,Q`, the coefficient scalings, a numerical convergence table, and a binary64 precision-loss warning. | READ |
| 31 | `q4/requirements.txt` | `mpmath==1.3.0`, `numpy==2.3.5`, `scipy==1.17.0`, `sympy>=1.13`. | READ |
| 32 | `q4/strike_completion_audit.md` | Strike-1 requirement-by-requirement completion table; explicit list of what was left unresolved; note that independent PF/geometry/filter reviews were performed. | READ |
| 33 | `q4/third_completion_audit.md` | Strike-3 completion audit: the stop taxonomy (E disproved, D and F not established, stop is G); a 24-row requirement table; direct inspection of the seven frozen JSON records; the publication-boundary argument; verified nine-field truth values; and **four named remaining mathematical escapes**. | READ |
| 34 | `reversible_reseed/requirements.txt` | `numpy>=2.0`, `scipy>=1.11`, `sympy>=1.12`, `mpmath>=1.3`. | READ |
| 35 | `review_2026_09_05/nullcline_exact_check.txt` | Two lines: PASS on rational section, cubic identity, transversality, sensitivities, determinant ratio; no ODE evaluations or periodic-orbit certificates produced. | READ |
| 36 | `staged_2026_09_05/PROFILE_SIGNS.md` | A 9×6 table of `sign log(R/r)` at `r=2,20,20000` over `c∈{0.6,0.7,0.825,0.93,0.95,0.965,1,1.2,1.5}` × `K∈{1/512,1/64,1/16,1/4,3/5,119/100}`, with `?` for unresolved returns. | READ |
| 37 | `staged_2026_09_05/fold_scope_review.md` | Independent scope review of the finite fold: return/unfolding evidence and the three-radius sign table; why the Newton Jacobian is the right fold diagnostic; the **exact rational** proof that the pair field's remote equilibrium is an *unstable* focus; the exact multiplier-density gate `N(0)>0>N(1)`; and why the fold and the large remote continuation cannot be combined. | READ |
| 38 | `staged_2026_09_05/return_review.md` | Architecture review of `compact_return.py` etc.: log-polar equations and the two clocks; curved-section orientation and the three guards added; flux-derivative correctness; augmented fold equations and acceptance limits; what the guards do and do not establish; infinity cross-check; and the final amended verdict on ledger entries 392–400. | READ |

No file in the list was left unread.

---

# PART B — NEW FACTS ONLY

## B0. Items on the specially requested topics

### Saddle loops / separatrix cycles around a weak focus (Lane C) and finite saddles

- **A whole KKL parameter box provably contains no finite saddle at all**, hence
  no finite saddle loop of any kind. `fold_surface_2026_09_05/theory_negative_review.md`:
  for `1/3≤c≤1` and `m≥37`, with `u=1+x` and
  `W(u)=(61/5-c)u³+(m-72/5+3c)u²+(11/5-3c)u+c`, the quadratic coefficient is
  `≥118/5`; the note proves `W>0` for `u≥0` and exactly one positive root of
  `W(-v)` on its final decreasing branch, where `W'(u)>0`, so
  `determinant = xW'(u)/u > 0`. *"There are exactly two finite equilibria, the
  origin and a single remote antisaddle x<-1; no finite equilibrium collision or
  saddle is present."* This covers **all audited fold and pair fields**.
- Nest separation, same file: *"The transverse line x=-1 separates any periodic
  orbit from the opposite nest."* Independently in `kkl/notes_geometry.md`: on
  `x=-1` the first component is identically `1`, so *"this line contains no
  equilibrium and cannot be crossed by a periodic orbit."*
- `staged_2026_09_05/return_review.md` §5 gives the dynamical use of that
  barrier: *"an exact remote trajectory returning to `x<-1` cannot have crossed
  to its right and subsequently crossed back."*
- `council/notes_third_routes.md` adds a second obstruction to the withdrawn
  reversible loop route beyond the missing external focus: *"The separate
  four-real-equilibria configuration theorem also obstructs its proposed `(4,1)`
  neighborhood."*
- Q4-side analogue: `q4/notes_pf.md` §2 and `q4/notes_lobe_second.md` §5 call
  `t=1` the **homoclinic endpoint** of the Q4 annulus, and the certificate audit
  (`q4/notes_certificate_second.md` §7) proves the behaviour *at* that loop:
  `Ω ~ c/[1152(1-a)^{3/2}v]`, `P ~ -cH(1)log(1/v)/[1152(1-a)^{3/2}] > 0`,
  `Y = -H(1)log(1/(1-t))/[576(1-a)] + O(1)`, hence `Z→+∞` but **`X(1)` is
  finite** — the loop contributes no extra original zero from divergence.

### Order-two weak focus

- **The KKL `K=0` origin is an order-two weak focus, with an exact fifth-order
  return coefficient.** `kkl/notes_return_review.md` §10 (hand-derived, no
  symbolic execution): at `K=0`, `m=210/(11c-5)`,
  `Δ = N2 - dN1/7 = 4J(c)/[5(11c-5)]` with `J(c)=305+634c-11c²-1000c³`, and
  ```
  R(r) - r = π Δ r⁵ / (120 m^{3/2}) + O(r⁶)   (K=0).
  ```
  Exact values `J(33/40)=103619/400` and `J(9/10)=13769/100` are positive, so
  *"the K=0 weak focus is repelling of order two at each of these shapes."*
  The derivation goes through the harmonic-energy coordinate
  `q=sign(z)√(2V(z))`, `q=√m x[1+Ax+O(x²)]`, `A=(10/m-2c-1)/3`, and uses
  `∫₀^{2π}cos⁴θ sin²θ dθ = π/8`; *"Lower odd damping terms do not change this
  leading even-term coefficient."*
- Immediate consequence (same section), a **no-collapse** statement:
  `R(r;c,K)-r = r³[K·A0(c,K,r) + r²·B0(c,r)]` with `A0,B0` analytic and
  positive after shrinking. *"Therefore no nonzero cycle can shrink into the
  origin through this local K≥0 region."* Explicitly disclaimed: *"The statement
  does not exclude finite-amplitude folds, escape, remote loss, or another K=0
  shape with a different J sign. No explicit numerical radius or
  parameter-neighborhood bound was certified here."*
- Q4 endpoint analogue of "order two": `council/notes_third_routes.md` repair 3 —
  *"Essential order two at that intersection does not mean five essential
  Melnikov coefficients: the cited theorem gives four."* (Buică–Giné–Grau,
  Lemma 5(iv),(vii), Theorem 6(iv),(vii).)

### Semistable cycles / folds

- **Which side of a fold produces which pair, and the precursor trap.**
  `kkl/notes_return_review.md` §5: with `p=p0+δv`, `A=∇_p L·v ≠ 0`,
  `B=L_uu ≠ 0`, `L = Aδ + (B/2)(u-u0)² + …`; the two-root side is `AδB<0`, the
  offsets are `±√(-2Aδ/B)`, and since `R_r=1+L_u` at each root, **`B>0` gives
  S/U (lower radius stable) and `B<0` gives U/S**. The load-bearing warning:
  *"A fold that annihilates the only known stable cycle is not a precursor. Nor
  does a fold equation locate an unseen disconnected pair automatically."*
  Also stated: three simple origin cycles in the intended configuration have
  successive displacement signs `+,-,+,-` with multipliers S/U/S.
- `staged_2026_09_05/return_review.md` §4 records a distinction the fold solver
  must respect: `F1=log|R/r|`, `F2=rR'/R-1`, and `F1=F2=0 ⟺ R=r, R'=1`, but
  *"`R'=1` alone does not imply `F2=0` unless `R=r`. That distinction does not
  invalidate a seed, but the seed must not be reported as an already stationary
  point for both coordinate choices."*
- `staged_2026_09_05/fold_scope_review.md` extracts unnormalized derivatives from
  the final finite-difference Jacobian
  `[[2.41e-9, -0.2137458],[2.89142e-5, -0.2048610]]`, `det ≈ 6.18e-6`, giving
  **`D_c ≈ -1.13513` and `D_rr ≈ 3.18e-6 > 0`** — i.e. `B>0`, so the pair is
  S/U with the stable cycle inside. Explicitly *"finite differences near, rather
  than rigorous derivative enclosures at, the final root."*

### Cycles from infinity

- `fold_surface_2026_09_05/theory_negative_review.md`: the vertical infinity
  point is **hyperbolic for all `0<c<1`** — in `X=x/y, V=1/y` its eigenvalues are
  `1-c` and `-c`. *"In particular c=1/3 is not a finite-m loss of hyperbolicity
  of that point. A putative endpoint there must involve the singular m=infinity
  limit, not just the finite-field infinity chart."*
- The same sheet **does** cross an infinity-direction collision, at `c=241/250`,
  where the discriminant `40c - 964/25` of the finite projective-slope quadratic
  vanishes; *"The fold remains finite while crossing this parameter line."*
- `staged_2026_09_05/return_review.md` §1: the staged integrator *"does not
  integrate through infinity: `w` remains an unbounded coordinate and has a
  finite guard"*; a guard termination is correctly `UNRESOLVED`.
- `council/notes_third_routes.md` §3 records that at `(a,b)=(-1,1)` the compact
  double-center cyclicity is two (Françoise–Gavrilov 2022, Thm 11) and *"the
  theorem explicitly leaves infinity cycles outside that count"* — so a
  mechanism must be two compact plus three endpoint, e.g. `(4,1)`.

### Explicit impossibility statements found in the gap files

1. **Two proposed births are impossible for a nonzero Q4 integral**
   (`q4/notes_geometry.md`): four simple zeros plus a double, and three simple
   plus a triple, both have total multiplicity six. *"Neither is possible… They
   cannot serve as starting configurations."*
2. **`P_B(r)≤0` ⟹ the entire dangerous two-root fibre fails** its required first
   positive maximum (`q4/notes_fifth_green.md`, boxed (G5-3)), equality included.
3. **The whole `β1=0` (`δ=0`) projective sector has at most three distinct
   original zeros** (`q4/notes_fifth_sign_chain.md` §6) — *"a complete
   lower-dimensional sector, not a continuity assertion."*
4. **`Y0=0` and `σY0>0` both exclude four original zeros**, regardless of later
   primitive geometry (`notes_fifth_sign_chain.md`, gate (T2)).
5. **Every multiple-primitive-root case is excluded**: a double + simple `H`
   root gives only one sign change, hence ≤3 distinct original zeros.
6. **The normalized `H(1)=0` endpoint baseline cannot have four distinct
   original zeros** for any finite lift (`notes_fifth_sign_chain.md` §7).
7. **No local S/U fold pair occurs on the `β=0` slice near the `K=J=0`
   organizer**, and a uniform lower bound on `K/|J|` in the stable-remote cone
   excludes an origin cycle shrinking to zero inside it
   (`kkl/notes_new_gate_review.md` §4).
8. **No finite saddle exists** in the `1/3≤c≤1, m≥37` box (above).
9. **Five original zeros are impossible in the fixed-λ, fixed-anchor-ratio joint
   endpoint regime** (`q4/notes_green_third.md` §6) — but see the four
   explicitly-uncovered escapes in B7.5.
10. `kkl/notes_new_gate_review.md` §2: *"Calling the organizer an order-three
    weak focus would be wrong: its return map is identically the identity locally
    at the exact center."*

---

## B1. Strike-1 material (`ASTRA_FIRST_STRIKE.md`, `q4/notes_geometry.md`, `q4/notes_pf.md`, `q4/strike_completion_audit.md`, `q4/data/astra_verification.txt`)

### B1.1 The stop-condition taxonomy the strikes run under
Never stated in the summary. Strikes may stop at one of A–F; strike 1 stops at
**C** ("a structural theorem substantially reduces the attack"); strike 3 stops
at **G** ("bounded high-value tasks exhausted"). `q4/third_completion_audit.md`
grades them: *"E is disproved by the exact threshold path and rational late-root
box. D requires both a forced positive maximum and resolution of the +2
mechanism; neither has been obtained. F requires closure of the whole
positive-Green-maximum architecture. A fixed-shape, fixed-positive-lambda
endpoint obstruction is narrower."*

### B1.2 The auxiliary evaluation curve lies on an explicit quadric
`q4/notes_geometry.md`: the auxiliary space is `V=span{1,t,M,tM}` and the
evaluation curve `[1:t:M:tM]` lies on `X0X3 = X1X2`. Its hyperplane sections are
intersections of `M(t)` with a fractional-linear function. Explicit disclaimer:
*"no such equation has been proved for `[B1:B2:B3:B4]`"* (the original basis).

### B1.3 All four auxiliary Wronskians, with the Cauchy–Schwarz proof
For `(1,t,M,tM)`: `W1=W2=1`, `W3=M''>0`, `W4=3(M'')²-2M'M'''<0`, proved by
**strict Cauchy–Schwarz** on the Stieltjes moments (strict because `u/(1-tu)` is
non-constant on the support). A Chebyshev-free restatement: with
`R(t)=t+2M'(t)/M''(t)`, `R'=W4/(M'')²<0`, and for `F=a+ct+dM+etM`,
`F''/M'' = d+eR` has at most one zero. Equivalent basis `(1,t,w,v)`, `v=-M`, has
`W3=w''<0`, `W4>0`.

### B1.4 Exact fold/cusp parametrization of the auxiliary space
`f''(t)=M''(t)(R(t)-b)`; `R(0)=54/31`, `R(1⁻)=1`; every `1<b<54/31` has exactly
one inflection `t_*=R^{-1}(b)`; **outside this range the curvature has one strict
sign, so three interior zeros are impossible**. The double-root locus is the
ruled surface `B=-M(t)-(t-b)M'(t)`, `A=1-(t-b)M(t)-Bt`, with triple edge `b=R(t)`
where `f'''(t)=M''R'<0` (each triple zero exactly cubic). Perturbation
`(A,B)↦(A-λt_*, B+λ)` splits the cubic zero into three simple zeros.

### B1.5 The Stieltjes representation's explicit density and its proof
`q4/notes_pf.md` §4 gives what the summary only names:
```
M(z) = ∫₀¹ ρ(u)/(1-zu) du,   ρ(u) = 3 / (2π² |F(1/u+i0)|²) > 0.
```
Proof ingredients not in the summary: the Euler integral
`F(z)=(1/2π)∫₀¹ u^{-1/6}(1-u)^{-5/6}(1-zu)^{-1/6}du` has argument in `(0,π/6)`
in the upper half-plane, so **`F` cannot vanish in the slit plane**; the Abel
identity `UV'-U'V = c/(x(1-x))` with **`c = -1/(4π)`** fixed from the upper-bank
endpoint expansion; `Im M(x+i0)=3/(2πx|F|²)>0`; `M(z)=O(z^{-2/3})` at infinity
kills the large circle; `ρ(u)` behaves as `const·u^{-1/3}` at `0` and
`const·log(1-u)^{-2}` at `1`; `M(1)=1`.

### B1.6 The `b_*(t)` inflection theorem via a covariance argument
```
b_*(t) = ∫u ρ(1-tu)^{-3} / ∫u² ρ(1-tu)^{-3} = 1/E_t[u],
dE_t[u]/dt = Cov_t(u, 3u/(1-tu)) > 0.
```
Hence `b_*` strictly decreasing, `b_*(0)=(25/432)/(775/23328)=54/31`,
`b_*(1⁻)=1`, and `q_b'' = 2V(t)(b_*(t)-b)` with `V>0`, so its unique zero is
simple. This is the exact mechanism behind the corrected strip
`(54-23κ)/31 < β0 < 1` that the summary reports only as a result.

### B1.7 Two source-reading traps in Zhao
- *"One must not copy Zhao equation (33) literally as a Taylor series: the
  quantities in (34) are derivatives, hence the Taylor expansion needs `1/n!`,
  and its common period normalization is suppressed."* Worked example: the
  quadratic Taylor coefficient of `hI00` is `C(13k-18)/(324k²d)`.
- The first Taylor coefficient vector of the four original area integrals at
  `s=k` is `C(2/(9k), -1/(3k), -1/(3k), 2d/(3k))`.
- The coefficient change required before using Zhao's α/β map:
  `m2 = -2μ2/3 - 2dμ3/(3k)`, `m3 = -2μ3/(3k)`.
- The leading `J1` constant left unspecified in the source is identified:
  `J1(s) = (1/2√d)log(432d/(s-1)) + O((s-1)|log(s-1)|)`.

### B1.8 What the PF reduction does *not* give (a scalar-ODE caution)
`q4/notes_pf.md` §7: for each fixed `μ` one can construct `M2` with
`M2∘L2∘L1(I)=0`, an order-≤5 scalar equation — **but `M2` generally depends on
`μ`**, so *"this is not a common order-five operator for all four basis
functions. A common order-four equation … can always be constructed where their
full Wronskian does not vanish, but using that tautology to claim disconjugacy
would be circular."* Also: `M2` can acquire apparent singularities at zeros of
`det(r,r1)`, and their absence is not assumed. In `s`, `L2` becomes
`s(1-s)D_s² - (1/2)D_s - 5/36` with exponents `(0,3/2)`, `(0,1/2)`,
`(-1/6,-5/6)` at `0,1,∞` — no internal singularity on `1<s<k`.

### B1.9 Original projective rank conditions and the maximal-contact lemma
`q4/notes_geometry.md`: five distinct prescribed zeros ⟺ `rank E5 ≤ 3` ⟺ all
four-row minors vanish. *"A fifth zero is not a single extra condition after four
freely chosen zeros: the fourth and fifth determinant conditions must both
hold."* **Maximal-contact uniqueness lemma**: any nonzero member with interior
zeros of total multiplicity five is projectively unique, those multiplicities are
exact, and it has no further interior zeros; every attainable five-condition
matrix has rank exactly three and every six-condition matrix rank four,
confluent matrices included.

### B1.10 The one feasible five-zero birth mechanism, exactly stated
Three distinct ordinary zeros plus one distinct double zero (multiplicity five)
**is sufficient**. Concretely: find `s1,s2,s3,t` with
`rank[B(s1);B(s2);B(s3);B(t);B'(t)] = 3`. Then the uniqueness lemma forces the
first three simple and `I''(t)≠0` *without separate derivative tests*, and since
`B1(t)=hI00 ≠ 0` throughout the annulus, varying `μ1` unfolds the double zero
transversely. **"No such original-integral configuration has been exhibited."**
This is the concrete, still-unexecuted five-zero target.

### B1.11 A shorter original certificate
*"For exact admissible `κ,μ`, six rational points in the annulus with rigorously
alternating signs already prove exactly five distinct simple zeros."* Derivative
enclosures and interval Newton become optional. *"The claim in the earlier
certification document that exactness also requires checking the complement can
therefore be shortened when Zhao's global bound is explicitly invoked."*

### B1.12 The cusp-exclusion theorem, in its precise scoped form
For every fixed interior triple-contact `t_*`, an open coefficient neighbourhood
has `Z(H_q;(0,1))≤1` and `Z(I;(1,κ))≤3` for **every** `κ>1`, via the exact
identity `F(κ-dt) = -(κ-1)²J1(κ)·H_q(t)`. The neighbourhood is `κ`-independent
because `q,M,P,H_q` are universal, but *"Its size may depend on `t_*`; no uniform
neighborhood as `t_*→0` or `t_*→1` is asserted."*

### B1.13 The lobe test in weighted-area form (an equivalent the summary omits)
With `L0..L3` the four positive weighted lobe areas, (L) is exactly
```
L1 > L0,    L2 > L1 - L0,    L3 > L0 - L1 + L2.
```
*"These are genuine magnitude requirements; three derivative crossings alone do
not supply them. They are independent of `κ` and can therefore be used before
selecting a center parameter."*

### B1.14 The Rolle chain reconstructed from Zhao Proposition 3(ii)
`q4/notes_geometry.md` writes out the step the campaign relies on:
`L2 G = (ℓ2/(py))·D[py² D(G/y)]`, with `y` chosen by `y(s0)=0, y'(s0)=1` at
`s0∈(1,a)` so that the Chebyshev property of the homogeneous space gives `y>0` on
`[a,b]`. Five distinct zeros of `G/y` ⟹ four of its derivative ⟹ three after the
second weighted derivative ⟹ three distinct zeros of `F`, made simple by
`Z(F)≤Z(g)≤3`, and anchored Rolle from `F(κ)=0` then gives three simple `g` zeros.

### B1.15 Strike-1 exact replay numbers
`det ∂(α1,α2,β0,β1)/∂(μ1..μ4) = 17843617792000(k-1)²/(6561k⁴)`;
`W4(1,t,w,v)(0) = 3705625/1451188224`;
`w = 1 - t/6 - 25t²/432 - 775t³/23328 - 305675t⁴/13436928 + O(t⁵)`;
companion center coefficient of `t⁴` = `-1616615/967458816`; period companion
residuals `1.148e-41`, `0`, `0` at `t=1/4,1/2,3/4`.

### B1.16 The Riccati equation
`6t(1-t)w' = (1-t)(2w-1) - w²`, `w(0)=1` — the universal object all the auxiliary
geometry sits on.

---

## B2. Strike-2 material (`ASTRA_SECOND_STRIKE.md`, `q4/notes_lobe_second.md`, `q4/notes_certificate_second.md`, `q4/data/second_verification.txt`)

### B2.1 The anchor map in explicit finite form
`q4/notes_lobe_second.md` formula (A): `(A,B,η)ᵀ = E(y)^{-1}·(K0-K3 at y1,y2,y3)ᵀ`
with `E(y)` the 3×3 matrix of `(K0, K1, -K2)` rows. *"Its determinant cannot
vanish: a null vector would give three primitive zeros with no `K3` term."*
The **exact interlacing** with the auxiliary roots is
`0 < x1 < y1 < x2 < y2 < x3 < y3 < 1`.

### B2.2 The boundedness proof and the strictness of the endpoint inequalities
`q(0)=A-1-η/6>0`, `q(1)=A+B-η<0`, `q'(0)=B+1/6-25η/432<0`; and since
`q'(t)=B+M+(t-η)M' < B+1` while `q'` must be positive somewhere, **`B>-1`**.
*"The endpoint inequalities are strict: an additional endpoint zero, together with
three interior zeros, would force two interior zeros of `q''` by ordinary Rolle,
contradicting its single curvature change."* These hold on the whole
three-simple-root region, not just the lobe region, so `L̄` is compact.

### B2.3 Four boundary mechanisms, with the exhaustiveness proof
(1) root reaches the center ⟹ the exact plane `A = 1+η/6`;
(2) root reaches the loop ⟹ the affine plane `9061A+6289B-2431η-7242 = 0`;
(3) two interior roots collide ⟹ `H=q=0`, an ordinary double primitive root, the
two lobe faces being `H(x2)=0` and `H(x3)=0`;
(4) three collide ⟹ `H=q=q'=0`, root exactly cubic, **auxiliary contact double,
not triple**. Exhaustiveness is proved by compactness of the closed simplex plus
the multiplicity bound. Crucial disambiguation: *"The generic weighted-lobe
boundary is **not** the auxiliary discriminant: when `H` has a double zero, `q`
usually has a simple zero there."*

### B2.4 Spine, deformation retraction, and its caveat
`(y1,y2,y3)=(c-r,c,c+r)` is a two-dimensional exact subfamily; `(1/2-r,1/2,1/2+r)`
is a one-dimensional spine. Mapping any triple to its symmetric projection and
interpolating in the convex `Δ` gives a **strong deformation retraction** of `L`
onto that spine. *"This does not imply that a separate five-original-zero
property, if nonempty, must meet the spine."*

### B2.5 `Y0<0` is structural, not sampled
From `A<η-B`, `B>-1`, `η>1`: `1326A+864B-2431η-102 < -1105η-462B-102 < -745`,
hence **`Y0 < -2235/1361360 < 0`** throughout the entire three-auxiliary-zero
region.

### B2.6 The exact `J_n(1)` recurrence and endpoint functional derivation
`[n(n+1)+5/36]J_n(1) = 1/(2π) + n²J_{n-1}(1)` (the `n=0` case omits the last
term), from `[t(1-t)F']' = (5/36)F` and `lim_{t→1}t(1-t)F' = 1/(2π)`. Values:
`πJ0(1)=18/5`, `πJ1(1)=738/385`, `πJ2(1)=113202/85085`. Substituted into
`H = -6t(t-η)(1-t)F - 6ηJ0 + (A+11+11η)J1 + (B-17)J2` this yields the boxed
`H(1)` functional the summary quotes without derivation.

### B2.7 The tail majorant used in every rational certificate
```
|H(t)-S_N(t)| ≤ f_{N+1}t^{N+1}/(1-t) · [ |A-1|t²/(N+3) + |B|t³/(N+4)
                + (6(N+1)/(6(N+1)-1))(t²/(N+3) + |η|t/(N+2)) ]
```
with `f_{n+1}/f_n = (6n+1)(6n+5)/(36(n+1)²) < 1`, `k_n = -f_n/(6n-1)`,
`d_n = 6n f_n/(6n-1) > 0`, and `tF(t)M(t) = F(t)-K(t)`. Certificate used
`N=256`, largest tail `< 5.473e-20`, first three `< 1e-30`.

### B2.8 The `10^-7` box estimate
Using `0<M≤1` and `F≤1/(1-t)`: `|ΔH(t)| ≤ 10^{-7}[t²/(1-t)+t³/(3(1-t))]`. Two
different reductions appear in the two files —
`≤ (1519/192)·10^{-7}` in `notes_certificate_second.md` §4 and
`≤ 1519/1920000000` in `notes_lobe_second.md` §3. **These disagree by a factor
of `10^{-7}` in presentation** (the second has the `10^{-7}` already folded in);
both conclude `< 8·10^{-7}`, so the certificate is unaffected, but the two
displays are not literally consistent as written.

### B2.9 The exact reconstruction forcing-sign check, spelled out
`q4/notes_certificate_second.md` §7: `D_h=(2s/h)D_s` gives `L_h = -(16s/h)L_s`;
hence the scalar `Y` equation has forcing `-H/[1152t²(1-t)]`. *"The opposite sign
printed in the source's equation (24) is incompatible with that substitution."*

### B2.10 The exact `5/11` first-maximum estimate, with all its constants
`q4/notes_certificate_second.md` §7 and `q4/notes_audit_third.md` §1:
```
∫₀^{p1} dt/(p y²) < (6/7)[(11/6)^{7/6} - 1] < 8/9,   using y(t)>(1-t)^{5/6}, p≥√(1-t)
P0 < (9/4)|Y0| - η/192,   |Y0|/η < 601/136136 < 1/216
Z(p1) < -m + (8/9)P0 < m - η/216 < 0
1/216 - 601/136136 = 395/1837836 > 0
```
and the only fractional-power comparison reduces to `11·3¹¹ = 1948617 < 2000000`.
The summary quotes the integer inequality but none of the surrounding constants.
In `a`-coordinates the second threshold reads **`a > 2593/21636`**.

### B2.11 The strike-2 Green shot's own numbers (distinct from strike 3's)
`q4/data/second_verification.txt`, `q4_green_shoot.py`:
`a = 189417314263391/400000000000000`, `κ = 400000000000000/210582685736609 ≈
1.899491397409134`; `P0 = 2.1646953843e-8`; four P roots at
`0.1444648725550713, 0.38212709215976703, 0.5838595137337097, 0.8100626745341928`;
`Z` at those roots `-0.004579179219…, -0.004579180186…, -0.004579178869…,
-0.004579200509…`. Diagnostic strings: `"four P crossings found"` /
`"fails: all four Z extrema are negative"`. **This shot's Z heights are
`≈ -0.0045792`, not in the summary's `-0.00236…-0.00394` range** — the summary's
range belongs to the eight strike-3 shots only.

### B2.12 The six anchor trials, with exact lobe data
Three anchor triples `(1/8,1/2,7/8)`, `(1/4,1/2,3/4)`, `(3/8,1/2,5/8)`, each at
`κ=2` and `κ=4`. All six pass the primitive-lobe screen; all six have
`sampled_sign_changes: 0` over 65 samples on `[0.01,0.99]`, with
`"Fixed compact samples are neither root counts nor endpoint exclusions."` Each
record stores exact `q_roots`, four exact `weighted_lobes`, exact primitive
values at the critical points, exact `primitive_at_one`, and the transported
`original_mu`. Independent original-area agreement at `κ=2, s=1.5`:
`2.168404344971009e-19`.

### B2.13 Other exact strike-2 quantities
`Exact exclusion lift κ = 3878922961/2916000000`, `r = -27095705/51954846`,
`P0 = -3056925605483331742344151782161/2151790655250172000064000000000000`;
`Exact all-kappa candidate-box exclusion margin = 79526371464733/1456000000000000`.

### B2.14 The conditional ordinary-fold reduction (strike-2's proposed shortcut)
After (S1) and (S2), an original ordinary double zero is exactly an extremum
height `X(v_j)=0` with `Y(v_j)=0`, `Y'(v_j)≠0`. Replacing one of the four interior
strict inequalities in (S3) by equality, keeping the rest and `X(1)>0` strict,
gives three simple originals plus that double; a transverse height change splits
it. *"This replaces five arbitrary interpolation equations by one extremum-height
shooting equation with explicit strict sign guards. No such admissible point was
constructed."*

---

## B3. Strike-3 material (`ASTRA_THIRD_STRIKE.md`, `q4/notes_threshold_third.md`, `q4/notes_green_third.md`, `q4/notes_audit_third.md`, `q4/third_completion_audit.md`)

### B3.1 The full eight-shot table (the summary gives only the range)
| path | r | tuned κ | first Z maximum |
|---|---:|---:|---:|
| affine gaps | .5 | 2.176993690 | -0.003934275289 |
| affine gaps | .75 | 2.585137225 | -0.003420052400 |
| affine gaps | .9 | 3.273485702 | -0.002994048345 |
| affine gaps | .99 | 5.563932424 | -0.002532118321 |
| affine gaps | .9999 | 8.107104320 | -0.002362670910 |
| power gaps | .6 | 2.411599781 | -0.003599582185 |
| power gaps | .75 | 2.975077238 | -0.003139178640 |
| power gaps | .9 | 4.231007907 | -0.002719393737 |
The second path is `(r, 1-(1-r)², 1-(1-r)³)`; initial probes used
`r=.5,.75,.9,.99` at `κ=1.137,2,4,16`. *"The last affine band is especially
narrow, so it is retained as a diagnostic, not promoted to a rigorous crossing
count."*

### B3.2 The four reverse-tangency lines, in detail
Pairs `(a,t*) = (.75,.8), (.75,.95), (.9,.999), (.99,.999)`. First two fail
elementary lobe inequalities (`B<-1` throughout `1<η<54/31` for the first;
`q'(0)<0` would need `η` beyond the strip for the second). The later two have
numerical `η` intervals `≈(1.02026738, 1.27360457)` and `≈(1.00040215,
1.47941770)`; a 257-value check on each found **only zero or one primitive
crossing, never three**. Stationary heights of `η(t)=-U(t)/V(t)`, found on a
fixed 1601-point determinant mesh over `0.01 ≤ t ≤ 1-1e-10` with loop clustering
and Brent refinement:

| a | t* | stationary t | stationary η | endpoint `H(1)=0` η |
|---:|---:|---:|---:|---:|
| .9 | .999 | .7748605960 | 1.0126713259 | 1.1413797115 |
| .99 | .999 | .9647058127 | .9910953746 | 1.0206129074 |

Ratio poles at `.9927647683` and `.9959388455`. *"A finite mesh can miss
stationary roots, including even-multiplicity roots."*

### B3.3 The confluent boundary shots
Four primitive triple contacts `H=H'=H''=0` at `t=.6,.9,.99,.9999`, built by
exact-form small linear systems and shot in `κ` to `P=0`; **every corresponding
`Z` was below `-0.002`**. Emphasised: *"This is a primitive triple contact with
an auxiliary double root, not the previously excluded auxiliary triple-root
cusp."*

### B3.4 The finite-kappa endpoint balance (a distinct balance from `a=1`)
`q4/notes_green_third.md` §4 — not in the summary:

| k | `P0*` | `∫₀¹ΩH*` | `P*(1)` |
|---:|---:|---:|---:|
| 8 | 3.4274121102e-5 | 4.3136198665e-5 | **-8.8620775635e-6** |
| 9 | 6.4983521363e-5 | 4.4651735479e-5 | +2.0331785884e-5 |
| 16 | 1.5230831113e-4 | 5.1569191547e-5 | +1.0073911958e-4 |

*"the diagnostic is consistent with a finite endpoint balance between eight and
nine, while (G2) proves another limiting balance at infinity."* Quoted errors
below `5e-14` but explicitly *"not rigorous enclosures."*

### B3.5 The exact corner coefficient transport (all four `μ`)
```
μ1* = 27(8k-9)/(49280(k-1)),   μ2* = -9(8k-27)/(24640k),
μ3* =  9(8k-9)/(24640(k-1)),   μ4* = -81(2k-3)/(49280k(k-1)),
```
with `μ3 = 2μ1/3` and `Y0* = -3/1232`. Also
`P0*(a) = (3/1232)[C_a - 77/36]`, `C_a = (3/2)(1+a)+r(a)`.

### B3.6 The `a=1` closed objects and the positive-series proof of the two moments
`y=(1-t)^{5/6}`, `p=1`, `Ω1=(1-t)^{-13/6}/(1152t²)`, `R1(t)=(3/2)[(1-t)^{-2/3}-1]`,
`P0*=1/14784`. The two cancellations follow from
`5F-36(1-t)F' = Σ_{n≥1} (5n/(n+1)) f_n t^n` plus termwise beta integration:
weight `(1-t)^{-1/6}` cancels `(5/6)_n` giving `5Σ(1/6)_n/(n+1)! = 1`; weight
`(1-t)^{-5/6}` cancels `(1/6)_n` giving `25`. Consequence: `P*(t)>0`, `Z*(t)<0`
for all `0<t<1`, `Z*` strictly increasing from negative to zero — *"a precisely
balanced boundary, not a positive first maximum."*

### B3.7 The scaled-forcing sign lemma (G4), with its proof
For `h(c)=e-Vc+Qc log c+Dc²` with three prescribed zeros `0<c3<c2<1`, largest
normalized to one, and `D>0`: **`-D<e<0`, `Q<0`, `0<V<D`**. Proof: `h(c)/c` has
three zeros, its derivative has two zeros `ξ1,ξ2∈(0,1)` with numerator
`Dc²+Qc-e`, so `-e/D = ξ1ξ2 ∈ (0,1)` and `Q=-D(ξ1+ξ2)<0`; `h(1)=0` gives
`V=e+D∈(0,D)`. Hence `e-Vc+Qc log c < 0` for `c≥1`.

### B3.8 The uniform kernel bound that fixes the matching constant
`|E_k(u)| ≤ C k^{-1} u^{-7/6}` on `εc ≤ u ≤ 1` **(G6)**, with `b=5/2^{4/3}`,
`C_O = 3/(5·2^{1/3})`, `n_k = C_O k^{5/6}/O_k = 1 - bk^{-2/3} + O(k^{-1})`, and
`O_s/(C_O s^{3/2}) = s^{-2/3}+bs^{-4/3}+O(s^{-5/3})`. The primitive with no free
constant is
`B_λ(c) = (6/5)c^{5/6} + 6bλ^{2/3}c^{1/6} - ∫_c^∞[ω_λ(v)v² - v^{-1/6} -
bλ^{2/3}v^{-5/6}]dv`, matched to the hyperbolic form
`B_λ = (2λ^{5/6}/C_O)[(23/18)J_O(x) - O(x)tanh x + O_x(x)/2]`,
`J_O(x)=(9/50)cosh(5x/3)+(9/2)cosh(x/3)`, via `O_xx-2tanh(x)O_x+(5/9)O=0`.
*"The matching constant must not be chosen freely."*

### B3.9 The exact endpoint asymptotics along the threshold path
`ε=1-r`, `L=log(432/ε)`, `U=A+B-η`, `v=η-1`, `E=2πH(1)`:
```
B = -17/77 + (9061/2772)U + (1105/462)v - (85085/99792)E          (E1)
U = εLQ,   v = (εL/6)[V-(L+1)Q],   E = ε²Le
e → -15/154,   V → 45/154,   Q → -45/(154 log2),   D = 30/77
η-1 ~ (15/(308 log2))εL²,   A+B-η ~ -(45/(154 log2))εL,   H(1) ~ -(15/(308π))ε²L
B+17/77 ~ (1105/462)(η-1),   A-94/77 ~ -(643/462)(η-1)
```
Coefficient determinant of the limiting anchor system: `(log2)/8 > 0`.

### B3.10 The convergence table and a hard numerical warning
| quantity | ε=1e-8 | ε=1e-16 | proved limit |
|---|---:|---:|---:|
| `(η-1)/(εL²)` | 0.05031104109 | 0.05924259353 | 0.07026112212 |
| `U/(εL)` | -0.2817134558 | -0.3417506558 | -0.4215667327 |
| `2πH(1)/(ε²L)` | -0.06325168565 | -0.07791223332 | -0.09740259740 |

*"At very late anchors, converting the universal coefficients to binary64 can
erase the primitive lobe margins: at `ε=10^-16` the natural primitive scale is
about `10^-31`. High-precision evaluation of a coefficient vector already rounded
to binary64 cannot recover that lost path information."* Also noted: coefficient
deviations are `O(εL²)` while primitive lobes are `O(ε²L)` — *"that mismatch
explains the numerical conditioning."*

### B3.11 The closed-moment evaluator (avoids near-loop quadrature entirely)
```
J0 = (36/5)t(1-t)F'
Jn = [t^{n+1}(1-t)F' - n t^n(1-t)F + n²J_{n-1}] / (n(n+1)+5/36),  n=1,2
K0 = J1,  K1 = J2,  K2 = 6J0 - 11J1 - 6t(1-t)F,  K3 = 12J1 - 17J2 - 6t²(1-t)F
```
API: `threshold_anchors(r)`, `primitive_basis_closed(t)`,
`from_primitive_anchors_closed(anchors)`, `coefficients_from_r(r)`,
`primitive_value_closed(t, coefficients)`. Agreement with independent positive
period series at `t=1/4,1/2,3/4`: within `5e-57` at 75 digits.

### B3.12 The late-root certificate's own enclosures and box
Frozen point `A=1210581187245108808/10^18`, `B=-125731163118386543/10^18`,
`η=1212211767298108636/10^18`, from `r=3/4` with exact anchors `(3/4,7/8,15/16)`,
`N=1024`:

| t | lower | upper |
|---|---:|---:|
| 23/32 | 0.000017278951342272812294524053 | …054 |
| 13/16 | -0.000014889259450679365959381558 | …557 |
| 29/32 | 0.000010156415192365481634868459 | …460 |
| 31/32 | -0.000093181306082441272143107063 | -0.000093181306082441099333759810 |

Box radius `10^-8` with `|ΔH| ≤ (122047/3072)·10^{-8} < 10^{-6}`; base margins
`>10^{-5}`; one replay 1.73 CPU s. `H` is positive throughout `(0, 5/11]`, with a
separate stored exact enclosure at `5/11`.

### B3.13 An exact moment form for the first Green maximum
`q4/notes_audit_third.md` §2, with `x(s)=asinh√(s-1)`,
`E(x)=[5cosh(x/3)-cosh(5x/3)]/4`, `O(x)=(3/10)sinh(5x/3)+(3/2)sinh(x/3)`:
```
R(t) = 2O(x(k))²/√(k(k-1)) · [ E(x(s(t)))/O(x(s(t))) - E(x(k))/O(x(k)) ]
Z(t) = Y0 + P(t)R(t) + ∫₀^t R Ω H
```
so at the first zero `p1` of `P`: `P0 = ∫₀^{p1}ΩH`, `Z(p1) = Y0 + ∫₀^{p1}RΩH`.
*"positive first maximum is exactly a weighted-moment inequality; its mean of `R`
must exceed `(-Y0)/P0`."*

### B3.14 Three documentation defects reported but not mathematical errors
`q4/notes_audit_third.md` §6: *"A companion-period notation collision, a missing
inner-interval qualifier on the Omega bound, and a prefactor-five ambiguity in
beta-sum explanatory prose were reported to the document owners; the mathematical
constants themselves check."*

### B3.15 A metadata (not mathematical) error and its fix
`q4/data/third_verification.txt` and `q4/third_completion_audit.md`: *"The first
metadata assembly expected `script_sha256` in all JSON records; the
reverse-tangency record instead uses `source_sha256`."* Corrected without
repeating the passing checks.

### B3.16 The four named remaining escapes after strike 3
1. finite, non-asymptotic points in the late-root lobe region outside the tested
   paths; 2. anchor ratios that themselves degenerate with `ε`;
3. joint limits with `λ=(1-a)/ε → 0` or `∞`; 4. other reverse-Green-tangency
lines in the two-variable `(a,t*)` family. *"No straightforward unexecuted
candidate promotion or certification step is visible: there is no positive
first-peak lead to promote."*

---

## B4. Strike-4 material (`ASTRA_FOURTH_STRIKE.md`, `q4/notes_N_compact.md`, `q4/notes_N_double.md`, `q4/notes_N_loop.md`)

### B4.1 The uniform compact margin — a formula, not just `δ=1/64`
`q4/notes_N_compact.md` (C6): using `F≥1` and `M ≥ 1/6 + (25/432)t`, and the
exact identity `W1H* = F(6M-1)[(1-t)^{-5/6}-(1-t)^{-1/6}]/(9856t)`,
```
Φ_a(τ1) < -(25/118272)(δ^{1/6} - (1/5)δ^{5/6}) < 0   whenever τ1 ≤ 1-δ,
```
**uniformly for every `0<a<1`, with no upper cutoff on `a`.** `δ=1/64` recovers
the summary's `-395/3784704`.

### B4.2 The two corrections strike 4 makes to its own proposed decomposition
`q4/notes_N_loop.md` §1: (i) the affine constant needs `K3 - K0`, not `K3`;
(ii) *"sending only the last anchor `y3` to one does not force the other anchors
to one and does not justify replacement by the corner primitive."* The
fixed-ratio path handles partial endpoint strata directly.

### B4.3 The exact operator conjugation and residual
```
L_a v = (1-at)(1-t)v'' - ((1+5a-6at)/2)v' + (8a/9)v,   v_a = y_{2,a}/(1-at)^{3/2}
v_1(t) = (3/2)[(1-t)^{-4/3} - (1-t)^{-2/3}]
L_a v_1 = (1-a)[22 - 7(1-t)^{2/3}] / (6(1-t)^{7/3}) > 0
W_a(t) = v_a(t)/(1152 t²(1-t)^{3/2})
```
Comparison mechanics: with `h_a = y_a/(1-at)^{3/2} > 0`, `w=v_1-v_a`, `z=w/h_a`,
integrating factor `ρ = h_a² exp∫(B_a/A_a)` gives `(ρz')'>0` with `z(0)=z'(0)=0`,
hence `z'>0, z>0`. *"It does not assume a maximum principle for an operator with
a positive zeroth-order term."*

### B4.4 Integrability bookkeeping at both singular endpoints
Near `t=0`: `H*=O(t²)`, `W1=O(1/t)`, product `O(t)`. Near `t=1` with `δ=1-t`:
`H*=O(δ²(1+|log δ|))`, `W1=O(δ^{-17/6})`, product `≤ Cδ^{-5/6}(1+|log δ|)`. For
finite `k`: `W_a=O(δ^{-3/2})`, product `O(δ^{1/2}(1+|log δ|))`. *"There is no
hidden divergent integral in this step."*

### B4.5 The cardinal-sign derivation
`∂H(t)/∂y_j = -H'(y_j)·ℓ_j(t)` with `sign(γ_j)=(-1)^{j-1}` from cofactor
expansion of the `(1,x,m)` interpolation matrix, and `sign H'(y_j)=(-1)^j`.
Two-column minors formed by `1,x` are positive in increasing node order.

### B4.6 The `S'` identity that makes convexity a one-line proof
```
S(t) = (M(t)-m(t))/(t-x(t)) = weighted average of secant slopes < M'(t)
S'(t) = (M'(t)-S(t))/(t - x(t)) > 0
```
*"Therefore `m` is a strictly convex function of `x`, without a separate
Wronskian or quadrature assumption."*

### B4.7 The exact endpoints of the moment curve
`x(0)=0`, `m(0)=1/6`, `x(1)=6289/9061`, `m(1)=2431/9061 = 11/41`,
`S(1⁻)=1105/462`. (Both forms of `m(1)` appear across the two files; they are
equal — `9061=41·221`, `2431=11·221` — so this is a notational, not a numerical,
inconsistency.)

### B4.8 The perturbation argument that upgrades "sign changes ≤3" to "distinct
zeros ≤3" on `L`
`q4/notes_N_compact.md`: four distinct zeros under the multiplicity-five bound
must be four simple, or three simple plus one ordinary double. The former
contradicts the sign-change bound; in the latter, perturb by the **nonvanishing
basis member `h·I00`** with the sign that splits the double zero. The other three
persist, the perturbation stays in the open region `L`, and five simple roots
would contradict Theorem N. *"This local argument uses only that `h` is nonzero
and `I00` is the positive oval area inside the annulus."*

### B4.9 **The explicit outside-lobe four-crossing sign pattern** (the sharpest
statement anywhere about the remaining gap)
`q4/notes_N_compact.md`, consequence audit. If `H` has two simple crossings with
signs `+,-,+`, the sign chain alone permits
```
P:  +, -, +, -        Z:  -, +, -, +, -
```
because `P'=-ΩH` makes its three monotone pieces decrease/increase/decrease and
`Z(0)<0` allows four crossings if the three extremal heights alternate. Verbatim:
*"Theorem N assumes three primitive roots and supplies no inequality on that
two-root stratum. This is an abstract allowed sign pattern, **not** a constructed
Q4 example. It identifies exactly why neither the first-lobe count nor ordinary
anchored Rolle proves the claimed global bound of three."*
Also, on the lobe region itself with `P0>0`: `P` has an even number ≤4 of sign
changes; if four, with roots `p1<…<p4`, then `Z(p1)=Φ(p1)<Φ(τ1)<0` so `Z` has no
zero before `p2`, leaving at most three.

### B4.10 Uniformity in `a` on compacts
`q4/notes_N_loop.md` §6: for `a` in any compact `[a_min,a_max] ⊂ (0,1)`,
dominated convergence with majorant `W1H*` makes `Φ*(1;a)` continuous, so its
strict negativity gives a **uniform** negative upper bound on that interval,
transferred to every strict anchor triple. *"No numerical value such as `-0.63`
is needed for the proof"* — an explicit rejection of an earlier numerical
constant.

---

## B5. Strike-5 material (`q4/notes_fifth_sign_chain.md`, `notes_fifth_two_anchor.md`, `notes_fifth_green.md`, `notes_fifth_original_height.md`, `q4/data/fifth_exact_checks.txt`)

### B5.1 The anchored sign-change lemma
If `X` has `n` distinct interior zeros then `Y` has at least `n` sign changes,
because on each `(x_{j-1},x_j)` the positive-weight integral of `Y` vanishes.
*"This argument counts distinct original zeros regardless of their
multiplicity."* Corollary chain: `k` sign changes of `H` ⟹ ≤`k+1` of `P` ⟹
≤`k+2` of `Y`; so **`H` with ≤1 sign change ⟹ `I` has ≤3 distinct zeros**.

### B5.2 The homogeneous coefficient normalization that includes `β1=0`
`H = αK0+βK1+γK2+δK3` with
```
Y0 = (9/3080)α + (162/85085)β + (3/560)γ + (459/170170)δ
Y1 = -(3/2)(1+a)Y0 + γ/192,     P0 = Y1 - r(a)Y0
```
*"in particular no division by `δ` is needed when `δ=0`."*

### B5.3 Complete endpoint classification when `H(1)=0`
`P_e = P0 - ∫₀¹ΩH` is finite. If `P_e≠0`, `Z→±∞` with the sign of `P_e` and `Y`
has a finite nonzero endpoint limit of that sign. If `P_e=0`,
`P(t)=∫_t^1 ΩH = O((1-t)log(1/(1-t)))`, `PR→0`, `Z(1)=Φ(1)` finite, and the sign
of `P` on the last lobe is `σ`. *"Such an endpoint zero of `P` is not an interior
crossing."* `X(1)` is finite in all cases.

### B5.4 The three gates, complete and both orientations
```
(T1)  σP0>0,   σP(τ1)<0,   σP(τ2)>0,   σP_e<0        (three simple P zeros)
(T2)  σY0<0,   σZ(p1)>0,   σZ(p2)<0,   σZ(p3)>0      (four Y sign changes)
(T3)  σX(v2)>0, σX(v3)<0,  σX(v4)>0,   σX(1)<0       (four original zeros)
```
Root order `p1<τ1<p2<τ2<p3`; `Y` signs `-σ,σ,-σ,σ,-σ`; `σX(v1)<0` automatically.
The last condition of (T1) is automatic when `H(1)≠0`. Necessity holds for **any**
four distinct original zeros, not only a generic four-simple proposal, and in
that case all four originals are in fact simple.

### B5.5 The non-generic-case sweep
A zero of `P` at a primitive root is a tangency, not a sign change (the `H` root
is simple); a zero of `Z` at a simple `P` root is an even-order tangency blocking
the two adjacent required sign changes; vanishing higher center derivatives
create no new monotone piece. *"an additional multiple original zero cannot hide
at an uncounted `Y` zero or evade the four inequalities in (T3)."*

### B5.6 The complete two-anchor fibre classification (7 cases)
`H_λ = B + λV = V(R+λ)` with `R=B/V` strictly decreasing, `R(1)=0`,
`R(0)=q_B(0)/(1/6-b)>0`:

| λ | interior roots and orientation |
|---|---|
| `λ>0` | exactly `r,s`, simple, `+,-,+`; `H(1)>0` |
| `λ=0` | exactly `r,s`, `+,-,+`; extra endpoint root `H(1)=0` |
| `-R(0)<λ<0` (except next two) | **three** simple interior roots — a strict lobe point |
| `λ=-R(r)` | ordinary double root at `r`, simple root `s` |
| `λ=-R(s)` | simple root `r`, ordinary double root at `s` |
| `λ=-R(0)` | `r,s` simple, signs `-,+,-`; center quotient vanishes |
| `λ<-R(0)` | `r,s` simple, `-,+,-`; `H(1)<0` |

The two double roots are exactly quadratic; the center primitive at `λ=-R(0)` is
exactly cubic (`q` has a simple center zero, the ECT property extending to `t=0`
because its Wronskians there are strict Stieltjes moment determinants). Crucial
terminological warning: *"the two double-contact levels … have two distinct roots
but total interior multiplicity three. They lie in the closure of the strict lobe
region and have only one primitive sign change."*

### B5.7 The proof that `R'<0`
If `R'(t0)=0` off the anchors, `B-R(t0)V` has a double zero at `t0` plus both
anchors — four interior zeros with multiplicity. At an anchor, `R'=0` gives order
≥3 there plus the other anchor. Both exceed the bound three.

### B5.8 Sharpened center-functional bounds
```
Y(U) = (9/3080)(α + Xβ + (11/6)γ + (204/221)δ),   X = 144/221
chord at X:  b + SX < 9889/37734
231/50312 < v := Y(V) < 3/616            (T7)
endpoint tangent values at X: 271/1326 and exactly 1/6
n(x) = K3/K0 strictly convex ((tM)'' = 2M'+tM'' > 0),  n(0)=0, n(1)=1819/9061
Y(E) > (9/3080)[204/221 - (1819/6289)(144/221)] = 27/12578 > 0     (T8)
0 < λ_c < η_B,  η_C = η_B - λ_c > 0                                 (T9)
```

### B5.9 The surviving branch, exactly
```
(T12)   0 < λ < λ_c < η_B,   H_λ(1) > 0,   H_λ: +,-,+,   Y0 < 0
```
*"This is a bounded interval on every exact two-anchor fibre. It is not a uniform
positive separation from the endpoint or from `λ_c`."* Two explicit
non-extensions: `Φ_λ(r)` has **positive** slope in `λ` and `Φ_C(r)=∫₀^r W_aC>0`,
so *"Theorem N cannot be extended as negativity of `Φ(r)` over this whole
interval"*; and *"`Φ_B<0` does not assert `Z_B<0` while `P_B>0`, because
`Z=Φ+PR`."* A further reduction: `P_λ(0)=-c_aY0(λ)-η(λ)/192` is affine and
strictly negative at `λ_c`, so if any intermediate point has the necessary
`P_λ(0)>0` then `P_B(0)>0` and the admissible range ends strictly before `λ_c`.

### B5.10 Baseline anchor monotonicity and the confluent reduction
```
∂_j(B/d) > 0,   η_B v_j - d > 0,   ∂_j(η_B/d) > 0        (T13)
∂_j(P_B(t)/d) < 0,   ∂_j(Z_B(t)/d) < 0   for 0<t≤r        (T14)
Z_B(0)/d = -1,   P_B(0)/d = c_a - η_B/(192d)
```
Consequence: *"at a fixed first anchor `r`, the normalized value `Z_B(r)/d`
decreases as `s` increases. An upper bound for that baseline value is therefore
reduced to the confluent endpoint interpolation `H_{r,r,1}` as `s` decreases to
`r`."* Immediately disclaimed: no sign for that boundary value, no monotonicity
in moving `r`, and *"Nor does it by itself settle the bilinear Green determinant
at the first anchor."*

### B5.11 The Green determinant `K` — the complete residual reduction
```
K(t) = P_B Z_C - Z_B P_C,   K'(t) = Ω[C Z_B - B Z_C]
K(0) = -Y0(B)P_C(0) < 0
K(t)=0 with P_B(t)>0, 0<t<r   ⟹  K'(t) > 0                      (G5-2, one-way)
K(r) = P_B(r)Φ_C(r) - Φ_B(r)P_C(r)                              (G5-4)
θ_e = P_B(r)/(P_B(r)-P_C(r)) ∈ (0,1),   θ'(t) = Ω[BP_C - P_B C]/(P_B-P_C)² < 0
```
Results: `P_B(r)≤0` ⟹ whole fibre excluded (equality covered);
`K(r)≤0` ⟹ whole fibre excluded (equality covered, via `Z_B(r)>0` and strict
positivity of `K'` just left of `r`); **`K(r)>0` ⟺ a positive first maximum
exists somewhere on the fibre**, realized on the interval
`θ_e < θ < θ(t0)` where `t0` is `K`'s unique interior zero. Lift-independent
initial value: `(P_B Z_V - Z_B P_V)(0) = -(η_B ℓ + Y0(B))/192 = -ℓη_C/192 < 0`.
Two extra exact necessary conditions in the residual lead: **`Z_B(r)>0` and
`Z_V(r)<0`**. *"A universal nonpositive bound for `Z_B(r)`, or a nonnegative
bound for `Z_V(r)`, would close the remaining first-maximum case."*
A source-ratio order also proved: `d(B/V)/dt < 0`, and the same for `B/C`.

### B5.12 The original-height functional and why it does not close the fibre
```
u = ∫₀^t y/(1-as)^{3/2},  j = ∫₀^t yR/(1-as)^{3/2},  U = u - j/R,  F = X - (j/R)Z
U' = jR'/R² > 0,   F' = U'Φ,   F(t) = ∫₀^t U'Φ
F(t) = U(t)Y0 + ∫₀^t K_t(s)Ω(s)H(s)ds,   K_t(s) = R(s)[U(t)-U(s)] > 0
∂_s K_t(s) = R'(s)[U(t)-u(s)]  — sign changes + to - exactly once
F(t) = X(t) whenever Y(t)=0
variation-of-parameters form:  X = uΦ + jP - ∫₀^t [Ru - j]ΩH
```
Interlacing: `Φ` has three simple roots `ξ1∈(v1,p1)`, `ξ2∈(v2,p2)`,
`ξ3∈(v3,p3)`, signs `-,+,-,+`, and these are the successive extrema of `F`, so
four original crossings force **`F(ξ2)>0` and `F(ξ3)<0`** (O6). Also
`F(v2)/U(v2) < Φ(r)` in both cases `v2>r` and `v2≤r` — but *"The remaining
two-root branch already permits `Φ(r)>0`, so this bound does not establish the
desired nonpositive original height."*
**The negative result**: `F_B(t) < 0 < F_C(t)` for every fixed `0<t≤r`, and
`F_λ(t)` is affine in `λ` with a unique zero strictly between `0` and `λ_c`.
*"Consequently there is no valid extension asserting negativity of this new
functional everywhere on the first lobe of the entire dangerous fibre."*

### B5.13 Strike-5 replay outputs
`check_fifth_two_anchor.py`: `ell_V_strict_lower 231/50312`,
`ell_V_strict_upper 3/616`, `eta_zero_center_strict_lower 27/12578`,
`moment_endpoint_slope 1105/462`, CPU `4.6e-5 s`.
`q4_fifth_green_checks.py`: *"Residual endpoint determinant sign remains OPEN; no
parameter signs evaluated."*
`check_fifth_loop_gate.py`: exact saddle-ray factorization, positive-area
Jacobian, denominator derivative, tangent-endpoint identity, affine quartic
numerator, and reconstruction from **all four ORIGINAL area basis terms** — all
PASS; *"No sign on the residual fibre or endpoint-cycle claim is asserted."*

---

## B6. Council third-route material (`council/notes_third_routes.md`)

### B6.1 Six repairs to the council, only one of which the summary records
1. **Linear dependence is neither a kill test nor an alien-cycle test.** Four
   independent first-order interior coefficient functionals are coordinates on
   the four-dimensional normal-control space, so *every* scalar first-order
   endpoint functional is their linear combination — an algebraic fact supplying
   no zero count. Conversely, *"an extra independent scalar coefficient would not
   by itself prove two endpoint cycles or compatibility with three interior
   cycles."*
2. **Alien occurrence is an existence result with hypotheses.** CDR treat
   particular Hamiltonian two-saddle unfoldings *including a persistent
   connection*; *"it does not assert an alien cycle at every two-saddle
   graphic."* Their one-saddle exclusion is about the Hamiltonian
   Abelian-integral comparison, *"not a license to identify arbitrary original
   Q4 graphics through a singular covering."* And from Gavrilov–Iliev 2015:
   **"two actual cycles can coexist with only one nearby Melnikov zero"** — the
   discrepancy concerns the full return map, not a count of parameter
   functionals.
3. **Generic ≠ symmetric base.** At the council's `λ=(0,1,3,-10,0,1)` the generic
   Darboux nonvanishing condition holds; the symmetric stratum needs `λ2=0`,
   which is the degenerate limit `r=0`, outside the proposed box. Essential order
   two there gives **four**, not five, essential Melnikov coefficients.
4. **Primitive roots are not original Melnikov roots** — the certified three-root
   boxes concern `H`, not `I`.
5. **Parameter counting is not a cyclicity theorem**, with a second explicit
   counterexample distinct from the one in the summary:
   `D(s,e) = e·∏_j(s-s_j)` *"can have any prescribed finite number of simple
   zeros."* Named as unestablished: *"neither 'three transverse directions
   forbids five' nor 'only the symmetric Q4 point has enough parameters' is an
   established exclusion."*
6. **Retain numerical-only status for the old infinity samples.**
Recommendation: *"The council's proposed dependent/independent-coefficient
cross-review trigger should be replaced by review of an actual original
displacement normal form or a proved obstruction."*

### B6.2 The concrete third-route family and its exact algebra (a seed)
```
X' = τX - Y - (2+r²)X² + [2(r+u)+w]XY + Y²
Y' = X + τY + (r+u)X² + [-1-3r²+v]XY - (r+u)Y²
```
Exact base `r=1, τ=u=v=w=0`:
```
X' = -Y - 3X² + 2XY + Y²,     Y' = X + X² - 4XY - Y²
```
Experiment limits (not exclusions): `r∈[1/2,2]`, `max(|τ|,|u|,|v|,|w|) ≤ 2^-8`.
Bautin parameters `(τ, r+u, 2+r², -5d+v, w, 1)` with `d=1+r²`; in normal-control
order `(τ,w,v,u)` the derivative of the four focus generators at the center is
```
diag(1, d, -5rd², 10r²d³),   det = -50r³d⁶ ≠ 0 for r>0
```
(`diag(1,2,-20,80)` at `r=1`). **At `r=0` this same derivative has rank two** —
the exact reason generic and symmetric targets cannot be exchanged.
Slope chart: `Q2(1,z) - zP2(1,z) = -(z+r)(z²+2rz-1)`; saddle directions
`z=-r±√d` with desingularized radial/angular eigenvalues `(d,-2d)`; the third
direction `z=-r` is a node with `(2d,d)`; antipodal time reversal changes
neither. Under GI's linear map `(x_G,y_G)=(-dX/4, dY/4)` the saddle slopes become
`y_G/x_G = r±√d`. At `r=1` use the algebraic directions `-1±√2` — no approximate
saddle location needed. Explicitly: *"This proves normal-control rank; it does
not prove rank or versality of the original endpoint unfolding."*

### B6.3 The four-step bounded preflight (a fully specified, unexecuted next step)
1. At exact `r=1`, identify the annulus boundary's saddle branches, finite
   connecting orbit and infinity connection; fix oriented transverse sections;
   write the full return as original Dulac plus regular transitions; check which
   connection persists under permitted quadratic perturbations.
2. Derive the genuine separation and saddle-ratio functions in `(τ,u,v,w)` and
   their transport to the four generating coefficients; record which exact ranks
   the unfolding needs. *"The focus-rank calculation above cannot substitute for
   this calculation."* (Marín–Villadelprat 2025 separation-derivative methods are
   available but their hypotheses must be verified on this graphic.)
3. Attempt original Melnikov anchors `t=1/4,1/2,3/4` at the same base: verify
   rank three of the original `3×4` evaluation matrix, a nonzero null direction,
   and three simple zeros. *"Failure is a failure of this seed, not all Q4."*
4. Only then allow the modulus box and original roots in `[1/8,3/8]`,
   `[3/8,5/8]`, `[5/8,7/8]` with gaps ≥ `1/16`; test a common
   connection-degeneracy condition retaining all three roots; then derive a
   parameter-dependent endpoint return expansion with a remainder strong enough
   to prove or obstruct two further roots.
A rigorous negative first result would be an obstruction to coexistence of the
interior-root condition and the endpoint two-root sector, or a uniform
closed-annulus zero theorem. *"Merely finding dependent leading coefficients, or
seeing no extra root in samples, cannot give that result."*

### B6.4 The certificate specification if a candidate is found
Freeze one exact rational quadratic field; five disjoint transverse intervals;
validated complete first returns with the stated compactified itinerary; opposite
displacement signs at interval endpoints; derivative separation from one, or
interval Newton, for each root; prove distinctness and nesting; independently
replay all five at the same parameter vector. *"An analytic existence argument
must likewise prove simultaneous roots and a parameter range before it can
replace a finite-field certificate."*

### B6.5 The resonant backup's exact obstruction at `(a,b)=(-1,1/5)`
```
x' = (b-2)/4 + ε1 x + (1-b)y + a x² + ε2 xy + b y²,     y' = ε0 - 2xy
```
*"the packet's exact first-normal calculation forces `ε0=0`,
`ε2=-(2/3)ε1` for the leading endpoint degeneracy, whereas the center trace then
equals `(2/3)ε1`. It obstructs that common nonzero first direction; it is not an
exclusion of every higher arc or blow-up."* Do **not** substitute the naive
"three small plus two endpoint" proposal there.

### B6.6 Four other rejected alternatives, each with its gate
- **Reversible finite-loop-plus-external-focus**: off-axis Jacobians are
  triangular with real eigenvalues; for the printed samples `k>0,p<0,q>0` the
  determinant is `-2kqY*² < 0`. Plus the four-real-equilibria configuration
  theorem obstructs the proposed `(4,1)`.
- **Finite Hamiltonian two-saddle alien example**: a useful validation control,
  but its closed-annulus theorem *"prevents five in that local construction.
  Local maxima cannot be added."*
- **Swirszcz 1999 reversible infinity bicycle**: a different graphic; abstract
  reports cyclicity two or three, *"this audit did not recover the original curve
  and complete hypotheses. There is no explicit common five-cycle unfolding to
  execute."*
- **Continuing Galias–Tucker**: *"a rigorous incumbent, not a fifth-cycle
  mechanism or a certified open parameter box. Its extreme separated scales
  increase continuation and certification cost relative to the finite-distance
  KKL proposal."*

### B6.7 The ordinal certificate-burden table (cost, `0` cheapest, `5` hardest)
| route, conditional on a numerical candidate | cost | reason |
|---|---:|---|
| KKL Hopf completion | 3/5 | finite-distance returns, explicit Hopf completion; large remote return and weak multipliers still need validation |
| Original generic Q4, three interior + two endpoint | 4/5 | original compactification, two saddle passages, small endpoint scales, common-parameter compatibility all need certifying |
| Resonant infinity, compact two + endpoint three | 5/5 | joint resonance remainder and compatibility missing before a candidate can even reach certification |
| Reversible loop + external focus as proposed | **Ineligible** | the stated equilibrium and configuration mechanism fails before certification |
| A hypothetical repaired reversible `(5,0)` | **Unrated** | no explicit eligible five-cycle target supplied |
*"These are ordinal engineering judgments, not probabilities or existence
evidence."* Conclusion: the Q4 preflight is preferable to the withdrawn reversible
proposal, *"It is not yet preferable to KKL on certificate readiness."*

---

## B7. KKL and staged/fold material

### B7.1 The exact uniformly-proved KKL segment (`kkl/notes_geometry.md`)
```
7/10 ≤ c ≤ 3/4,   α(c) = -216/(11c-5),   β=0,   K = 6/5 throughout
```
Proved for **every** parameter on the segment, not sampled: exactly one nonorigin
real equilibrium, hyperbolic stable remote focus, unchanged infinity portrait.
Machinery: with `s=-x`, `A=-α`, the equilibrium equation is `A=F(s,c)`,
`F = 10s + (11/5)s²/(s-1) - cs³/(s-1)²`; `s<0` gives `F<0`; `0<s<1` gives `F<10`
(vs `864/13 ≤ A ≤ 80`); for `w=s-1>0`,
`w³F_s = (61/5-c)w³ + (3c-11/5)w + 2c > 0` uniformly, so exactly one simple
remote root. Then `F(5,c) ≤ 1865/32 < 864/13` and `F(7,c) ≥ 19397/240 > 80` give
**`5<s*<7` uniformly**, `25/4<y*<49/6`, and
```
tr J* = s[(1+2c)s/(s-1) - 21/5] ∈ (-49/5, -43/8)
det J* = s(s-1)F_s > 185,   (tr J*)² - 4det J* < 2401/25 - 740 = -16099/25 < 0
```
Infinity: `Q2(1,z)-zP2(1,z) = -10+(6/5)z+(c-1)z²`,
`Δ_∞ = (1000c-964)/25 ∈ [-264/25, -214/25] < 0`; vertical direction eigenvalues
`(-c, 1-c)`, a saddle; margins to the two portrait boundaries at least
`241/250-3/4 = 107/500` and `1/4`.

### B7.2 Exact algebraic gates for later continuation
```
trace-zero boundary:  c = 8/5 - 21/(10s)
N(s,c) = (61-5c)(s-1)³ + (15c-11)(s-1) + 10c        (det J* = sN/[5(s-1)²])
E(s,c) = s[(10c-16)s+21]² - 20N                     ((trJ*)²-4det = sE/[25(s-1)²])
```
So `N=0` is the equilibrium-multiplicity/determinant boundary and `E=0` the
focus-node boundary. Warning: *"At nonzero beta, use the original Jacobian and
cubic; the beta-zero formulas must not be reused unchanged."*
Continuation protocol: Sturm isolation at exact rationals; rational interval
evaluation of `trJ`, `detJ`, `(trJ)²-4detJ` after substituting `y=-x²/(1+x)`;
*"An unresolved sign is not a type certificate."* Crossing `c=241/250` or `c=1`
*"requires a new compactified itinerary analysis; it is not an ordinary failed
finite return."*
Path tangent `dα/dc = 2376/(11c-5)² > 0`, `ds*/dc < 0` (negative term ≥ `38016/169
> 224`, positive term ≤ `343/36 < 10`), mesh `c_j = 7/10 + j/320`,
`α_j = -69120/(864+11j)`, `j=0..16`.

### B7.3 The `c>1` bounded test and its four negative controls (`kkl/NEXT_BOUNDED_TEST.md`)
Field: `ẋ=y+x²+xy`, `ẏ=-10x²+(11/5)xy+(1001/1000)y² - (196/5)x`, `β=0`,
`K = 32039/6250 > 1/64`. Rationale: across `c=1` the quartic `N` has **two
positive zeros in `u=1+x`, both greater than one, with a bounded negative
interval between them** — a different amplitude structure from the `c<1`
component. Calls 203–206, all full numerical returns:

| initial r | D(r) | R_r(r) |
|---:|---:|---:|
| -8 | +0.1934757492 | 0.9382092882 |
| -512 | +259.8321636 | 0.3479611859 |
| -32768 | +29886.64503 | 0.0416319720 |
| -1048576 | +1037240.44514 | 0.0035049036 |

*"All four displacements point inward on this section. There is no U-root sign
bracket to refine, so the initial test stopped after four calls and the
conditional origin block did not run."* Determinant vs speed/divergence
derivatives agreed to `<1.6e-12`. Budget: 206 charged, **3890 of the inherited
allowance remaining** at that point. The protocol's stated positive result would
be *"a remote U root plus an additional origin stationary branch"*; the actual
precursor gate remains three ordered origin S/U/S roots plus a remote U root at
one common field, then the negative-`β` step and five return certificates.

### B7.4 The second center of the `K=J=0` organizer, located exactly
(`kkl/notes_new_gate_review.md` §2). With `σ = 5(1+2c)/21`, the reversing
involution's fixed line is `x+σy=0`; intersecting it with the first nullcline
gives the **other** equilibrium
```
x* = -1/(1-σ),     y* = 1/[σ(1-σ)],     0<σ<1  so  x* < -1,   trace zero
```
*"Both equilibria have nonzero imaginary linear eigenvalues and are fixed by the
reversing reflection. Nearby rotating half-arcs join their reversed reflections to
make periodic orbits, proving that both are centers."* The summary calls this a
"double center" without the second center's location or this argument.
Polynomial replay avoiding floating approximation of the algebraic root:
`M=N/L`, `L=1000c²+1231c+145`, `N11=1000c²+769c+355`,
`N12=-10(1+2c)(11c-5)`, `N21=-2100(1+2c)`, `N22=-N11`; the replay verifies
`N²=L²Id`, `det N = -L²`, linear anticommutation, and every coefficient of
`F2(Nz)+LNF2(z)` **modulo `J`**. The first implementation attempted nested
rational-matrix expansion and **hit the 10 s CPU fuse**; the polynomial version
passed in `0.247154` CPU seconds. Also: `A = (8/5)conj(B)`,
`Im(conj(B)³C) = J/(35280√m)`, with the reduction
`250(1+2c)²(1-c) - (11c-5)(c+11) = J`; reversal identities
`Aλ=-conj(A)`, `B=-λconj(B)`, `C=-λ³conj(C)`; `J(241/250)=25281/2500>0`,
`J(39/40)=-11333/800<0`.

### B7.5 Remote-Hopf sign, alternative forms, and the Liénard route
```
K_H = -441J/(125 d e²) = -s_H³J/(105 w_H²),   d=16-10c, e=1+2c, s_H=21/d, w_H=5e/d
negative remote trace  ⟺  K > K_H   (in the simple-remote regime)
Hopf numerator:  B's[2(1-c)F_s + wF_ss] = 882J/(625e³),
   using  5e²(1-c)(61-5c) - c(c+1)d² = J
l1_z = 441J/[5000e³ρ²D^{3/2}],   l1_x = 441J/[5000e³D^{3/2}],   ρ=(-1-x)^{-c-1}
```
*"Both formulas have the sign of J."* Local unfolding: `D_return = r³[Ka + Jr²b]`
with `a,b>0`; for fixed nearby parameters `r²b/a` has positive derivative
(`2rb(0)/a(0)>0`, rest uniformly `O(r²)`), so at most one small isolated nonzero
root, requiring `KJ<0`, with displacement-derivative sign = sign `J`. **"No local
S/U fold pair occurs on this beta=0 slice."** In the stable-remote cone `K>0,J<0`
a positive lower bound on `K/|J|` conflicts with `r² = (K/|J|)a/b` where
`a/b → 75(11c*-5)/2 > 0`, so *"Uniform lower bounds therefore exclude an origin
cycle shrinking to zero inside this cone."* — explicitly local, no radius.

### B7.6 The full return-derivative apparatus (`kkl/notes_return_review.md`)
- Moving-event projection `Π_T = Id - F_T(0,1)/g_T`; `T_j=-w_{j,y}(T)/g_T`;
  `R_j = w_{j,x}(T) - (f_T/g_T)w_{j,y}(T)`. *"Using only the x component of `w_j`
  omits the moving crossing time."*
- Radial identity `R_r = [g(r,0)/g(R,0)]·exp(I(T))`, positive on one continuous
  downward itinerary. *"Away from a fixed point the bare divergence exponential
  is not the section derivative."*
- Intermediate-crossing convention: continue the entire augmented fixed-time
  state through the opposite crossing and apply `Π` only at the final return.
  *"Do not project at the first crossing and then treat those data as the
  original unprojected fixed-time sensitivities. Discarding/reinitializing
  sensitivity columns at the intermediate event is also incorrect."*
- Second-derivative formulas `w_ij' = Jw_ij + Hess F[w_i,w_j] + J_iw_j + J_jw_i`
  and the correct event correction
  `E_ij = w_ij + (Jw_i+F_i)T_j + (Jw_j+F_j)T_i + (JF)T_iT_j`;
  *"Projecting `w_ij` alone is not the correct second return derivative."*
- A named easy mistake: integrating in `u=log r` gives `w_u(0)=(r,0)` **and
  `w_uu(0)=(r,0)`** — the second initial derivative does not vanish.
- Residual conventions `L`, `G=(R-r)/r`, `H=R-r` agree at a fold but differ off
  it; *"These conventions cannot be mixed in a Newton corrector."*
- The determinant reformulation: `h_j = det(F,w_j)`, `h_j' = div(F)h_j +
  det(F,F_j)`, `h_r(0)=-g(r,0)`, `h_c(0)=h_α(0)=0`, and `R_j = -h_j(T)/g_T`. This
  proves the determinant method equals the projection method and *"specifically
  removes the potentially severe final subtraction `w_x-(f/g)w_y`; it is not a
  general conditioning theorem."*

### B7.7 The six named controls with their saved numbers
1. **Linear-focus analytic check**: with quadratic terms suppressed and
   `ν=√(-α-β²/4)`, `R = exp(πβ/ν)r`, `T=2π/ν`, `R_r = exp(πβ/ν)` while the bare
   divergence exponential is `exp(2πβ/ν)`; at `β=0`, `R_r=1`, `R_c=R_α=0`,
   `T_α = π/(-α)^{3/2}`.
2. Same-orbit derivative identity at a **non-fixed** section point (a root-only
   test cannot detect a missing speed correction).
3. **KKL incumbent control** at `(c,α,β)=(7/10, -363889/5000, 3/2000)`:
   `(0.683210217, 0.999226903)`, `(2.183699825, 1.002420055)`,
   `(15.962783982, 0.962020810)`, remote `(-3711.560806, 11.46226773)`.
   **This is the provenance of the disputed "remote cycle near 3711" that
   `FRONTIER_AUDIT` item B4 could not reproduce** — recorded here as a saved
   numerical reference with the explicit caveat *"not exact expected equalities
   or interval bounds. The two weak multipliers require relative error well below
   their distance from one before inferring their stability numerically."*
4. **K>0 starting control** at `(7/10,-80,0)`, `K=6/5`: origin root
   `≈64.555434341`, multiplier `0.809691136`; remote `≈-5391.141160`, multiplier
   `12.168019`. *"These are one-origin-plus-one-remote controls, not the desired
   precursor."*
5. **β=0 negative control**: the incumbent shape at `β=0` has
   `K=-674997/250000`, roots `≈3.068845425` and `15.064071451`, multipliers
   `≈1.005012827` and `0.967338367`. *"A spurious persistent third root near the
   origin is a warning."*
6. A small charged derivative spot-check; *"Near-escape or nearly tangent
   sections are unsuitable."*

### B7.8 Small-amplitude asymptotic checks and a counting warning
```
R - r = C r³ + O(r⁴),        C = πK/[4(-α)^{3/2}]
R_r = 1 + 3Cr² + O(r³)   vs   bare divergence exponential  1 + 4Cr² + O(r³)
R_c = [πb/(4√(-α))]r³ + O(r⁴)
R_α = (π/4)[-(bc-1)/ω³ + 3K/(2ω⁵)]r³ + O(r⁴)
```
*"No explicit remainder radius is proved here, and cancellation at very small `r`
can defeat floating-point verification. **Do not count arbitrarily tiny numerical
roots as cycles.**"*

### B7.9 Ledger forensics at the 202-call checkpoint
202 sequential charged evaluations; the first 64 reproduce `1.523849 s` evaluator
and `20.881385836 s` subprocess wall; totals `6.683029` / `65.515520670 s`; 24
attempted common-parameter records, 22 accepted; two rejected points record a
derivative discrepancy before the determinant upgrade and the remote
section-range boundary. **"The first determinant-output record is 132."**
Historical records carry no evaluator hash or timestamp; *"Frozen current-source
hashes must not be represented as contemporaneous hashes of the historical
evaluator versions."* Last accepted exact field
`(c,α,β) = (9301/10000, -8403125/209244, 0)` with `K=1/64`; evaluations 183 and
186 support the stable origin and unstable remote roots; the conservative
boundary sign pair is `c=0.9301046126889` / `c=0.9301046326889` at fixed
`r=-2^20` with displacements `≈-0.565318` / `+0.565357`. Evaluation 202:
`R_r≈1`, **`R_rr ≈ -0.001122065`**, displacement `≈+0.242693252`.

### B7.10 The staged return-map review (`staged_2026_09_05/return_review.md`)
- Log-polar equations, `dt/dτ=(1+ρ)^{-1}`, `div F = (21/5)x+(1+2c)y+β`,
  `L_τ = [ρ((21/5)C+(1+2c)S)+β]/(1+ρ)`, `T_τ = (1+ρ)^{-1}`. *"`period` means
  physical time and `rescaled_period` means elapsed numerical desingularized
  time. Neither can be substituted for the other when classifying infinity
  behavior."*
- Section `γ(r)=(r, -r²/(1+r))`, event expression `P/ρ² = C²+CS+Se^{-w}`, and on
  that set `dP/dt = (1+x)Q`; origin-side `x>0,Q<0` gives negative event
  direction, remote-side `x<-1,Q<0` gives positive.
- **A real bug found and fixed**: the initial code inferred the branch from
  `r>0`/`r<-1` without checking initial `Q<0` and without asserting matching
  nonzero final flux. *"For an arbitrary remote input, `r<-1` alone does not
  imply the input is left of the remote equilibrium or on the chosen minimum
  branch."* The final source implements all three checks (`r<x_*`, initial
  `Q<0`, final `Q<0`) and was used in ledger entry 398.
- Flux derivative `R'(r) = det(γ'(r),F(γ(r)))/det(γ'(R),F(γ(R)))·exp L`; on both
  sections the determinant is `Q`; *"The absolute value would silently discard a
  negative flux ratio."* `dq(R)/dq(r) = R'(r)(q(R)/q(r))²`.
- *"**Off a fixed point, `exp L` is only a divergence factor**, even though the
  generic result dictionary retains `multiplier`."* The final source now also
  returns `divergence_exponential` plus the string "Floquet multiplier only at a
  fixed point"; the same warning applies to transport rows.
- Derivative validation vs centred differences: `≈2.24e-8` (origin control) and
  `≈-6.65e-9` (cutoff remote control).
- `refine_kkl.py` dividing both equations by `(1-q)² = (|r|/(1+|r|))²` removes
  automatic small-radius quadratic vanishing without altering finite roots.
  *"The saved initial fold attempts all ended at `ITERATION_LIMIT`, and none met
  their candidate gates; no false fold was asserted there."*
- `correct()` accepts from a residual gate before proving a bracket:
  *"Near `R'=1`, a small residual can mean a much larger radius uncertainty."*
- `min_xy`, `max_xy`, `max_log_radius` come from accepted solver nodes:
  ***"sampled extrema, not enclosures of the entire orbit"*** — they cannot
  certify the theory's maximum-of-`x` requirement, annulus separation,
  equilibrium avoidance, or noncrossing.
- *"The code parses rational requests into binary floats… A final
  coefficient-alone interval verifier must use the rational coefficients directly
  and not treat the float parser as exact arithmetic."*
- Final tally: **exactly 400 charged KKL evaluations**; entry 392 the refined
  fold (`D≈-9.59e-14`, `D_r≈6.66e-16`), entry 393 the Cartesian replay
  (`D≈-1.40e-10`, `exp L≈0.999999999943145`), entries 394–397 the pair signs,
  entry 398 the guarded large-radius profile (positive; *"one such profile
  neither finds nor excludes an additional outer stable cycle"*), entries
  399–400 replays at `c=0.9683, K=1/64` with relative log displacements
  `≈-1.18e-9` and `3.55e-9`. Amended verdict: *"a statement that no fold was
  found would now be incorrect."*

### B7.11 The fold scope review's exact rational remote-focus proof
(`staged_2026_09_05/fold_scope_review.md`) The pair field read as exact rationals:
```
c = 4844447396953323/5000000000000000,   K = 1/512,
m = 1050048828125000000/28288921366486553,   α=-m,  β=0
```
`J(c)<0` and `K < K_H(c)` with **`K_H ≈ 0.0383581568420`** (the unperturbed fold
field's value is `≈0.0382154`), so the remote trace is positive. Independently,
without any orbit integration: `s_- = 3324674/10^6`, `s_+ = 3324675/10^6`,
`F(s_-) < m < F(s_+)`; `0 < t(s_+) < t(s_-) < 6/1000`;
`F_s(s) ≥ 10 + 11s_-(s_--2)/[5(s_+-1)²] - cs_+²(s_+-3)/(s_--1)³` giving
`det = s_*(s_*-1)F_s > 88`, hence `t²-4det < 0.006² - 352 < 0`. **"These exact
comparisons prove an unstable remote focus for the rational pair field."**
Scope: *"It does not exclude a stable remote cycle or any other finite-amplitude
remote-cycle mechanism."*

### B7.12 The exact multiplier-density gate at the pair field
With `W = m + (2m+10)x + (m+111/5)x² + (61/5-c)x³` and
`N = {du + (c+1)(21+dx)}W - u(21+dx)W'`: exact evaluation gives
**`N(0) = 5/512 > 0` and `N(1) < -180 < 0`**, both `x`-values inside the
candidate's recorded x-range. *"Thus the necessary positive/negative density
sampling for a multiplier-one cycle is allowed. This does not prove the weighted
integral vanishes."*

### B7.13 Other fold-review facts
Recorded fold: `D≈-9.6e-14`, `D_r≈6.7e-16`, winding `≈-1`, period
`≈0.9152566646`, x-range `≈[-0.552524, 6.949088]`. Pair-side Cartesian signs at
`c_f+10^-6`: `+6.81228e-6` at `r=4`, `-1.13527e-6` at `r=6.949087993605231`,
`+6.21330e-5` at `r=12`; the middle sign becomes positive at `c_f-10^-6`.
*"A single positive middle sample does not prove absence of cycles on that side."*
Newton residual scaling by `s=(1-q)²>0` with `L_c = D_c/r` and `L_zz = rD_rr` at
the zero: *"a nonsingular two-dimensional Jacobian with nonzero `L_c` and `L_zz`
is the correct ordinary-fold diagnostic, not an artifact of the log coordinate."*
The `c=0.9683, K=1/64` remote replay: section coordinate `≈-6.5372e9`, relative
residual `≈3.55e-9`, physical period `≈0.56721`, numerical multiplier `≈1.010887`
— and *"Its large **absolute** return residual, about 23 at a coordinate of order
`10^9`, should be reported together with the relative residual."*
Closing constraint: **"The finite-fold candidate and the large remote-cycle
continuation occur at different parameter vectors. They cannot be combined into a
coexisting cycle count."**

### B7.14 `staged_2026_09_05/PROFILE_SIGNS.md` — a data table not in the summary
Signs of `log(R/r)` at curved-section `r = 2, 20, 20000`:

| c \ K | 1/512 | 1/64 | 1/16 | 1/4 | 3/5 | 119/100 |
|---|---|---|---|---|---|---|
| 0.6 | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− |
| 0.7 | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− |
| 0.825 | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− |
| 0.93 | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− |
| 0.95 | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− | +,+,− |
| 0.965 | +,+,− | +,+,− | +,+,− | **+,+,+** | +,+,+ | +,+,+ |
| 1 | **−,−,+** | −,−,+ | −,−,+ | +,+,+ | +,+,+ | +,+,+ |
| 1.2 | −,−,? | −,−,? | −,−,? | −,−,? | −,−,? | **+,−,?** |
| 1.5 | −,−,− | −,−,− | −,−,− | −,−,− | −,−,− | −,−,− |

`?` = unresolved return. Only two rows show a sign pattern admitting a cycle
between `r=20` and `r=20000` in the direction sought; every `c=1.2` entry at
`r=20000` is unresolved. Caveat in the file: *"These samples do not cover the
parameter or return domain."*

### B7.15 The negative/actual-m sheet review (`fold_surface_2026_09_05/theory_negative_review.md`)
- Hash-fixed snapshots: 20 accepted fields + 1 unresolved in `events_negative`,
  6 + 1 in `events_m`, 11 + 1 in the completed `events_logm`. Every accepted
  field has two ordered opposite-sign root brackets, negative `G_z`, and the
  recorded unstable/stable ordering. *"**None of these saved profiles triggers
  three origin cycles; the first requested (3,1) configuration has not been
  established.**"* Outcome label: **PARTIAL**.
- Latest audited log-m field: `r ≈ 1.19703159003e10`, `m ≈ 2.76644661952e12`,
  `c ≈ 0.33668600865535249`; the independent full-return reproduction is at
  `c ≈ 0.33668739232461736`, same `m`.
- The complete-return bracket table (independent flux discrepancies `≈1e-24`):

  | bracket radii | left log displacement | right log displacement | endpoint derivative signs |
  |---|---:|---:|---|
  | 1.031660e10 → 1.053643e10 | -2.29867e-5 | +1.20739e-6 | both positive |
  | 1.355000e10 → 1.411362e10 | +5.01111e-7 | -6.41798e-5 | both negative |

  First bracket consistent with an unstable cycle, second with a stable cycle.
- Exact remote Hopf threshold in `m` (chart-free):
  `m_H(c) = 21(1000c²+1021c+481)/[50(1+2c)²(8-5c)]`. All stored folds and pair
  fields have `m > m_H(c)`, so the remote trace is negative. **The `c=5/11` pole
  is a chart artifact**: the threshold there is `1738/75` while the continued
  fold has `m ≈ 381.86`; every `m` on that line maps to `K=-42`, and
  *"the inverse formula `m=5(K+42)/(11c-5)` loses the parameter rather than
  detecting a dynamical boundary."*
- Origin data: `det = m > 0`, trace zero, first Lyapunov sign =
  sign of `K = m(11c-5)/5 - 42`, negative at every audited field. Remote trace
  `(v+1)[(1+2c)/v - (16-10c)/5]`; the unique remote `v` increases with `m`.
- **Large-m center scaling and its Melnikov integral** (the `c=1/3` organizer):
  ```
  y = √m Y,  τ = √m t,  ε = m^{-1/2}
  x' = (1+x)Y + εx²,   Y' = -x + cY² + (11/5)εxY - 10ε²x²
  at ε=0, 0<c<1/2:  H = u^{-2c}[Y² + au + b],  a = 2/(1-2c), b = 1/c,
  integrating factor 2u^{-2c-1};  both end potentials → ∞, ovals at arbitrarily large E
  M(E) = (4/5)∫ u^{-2c-2}(u-1)((16-10c)u + 5+10c)√(E u^{2c} - au - b) du
  ```
- **Why `c=1/3`**: endpoint contributions `C_R E^{p_R}` and `-C_L E^{p_L}` with
  `p_R = (3-4c)/(2-4c)`, `p_L = (2c+1)/(2c)`,
  `C_R = (4/5)(16-10c)a^{1/2-p_R}B((1-c)/(1-2c),3/2)/(1-2c)`,
  `C_L = 4(1+2c)b^{1/2-p_L}B((c+1)/(2c),3/2)/(2c)`,
  `p_R - p_L = (3c-1)/[2c(1-2c)]`. **At `c=1/3` both exponents equal `5/2` and
  `C_R = 152/675`, `C_L = 8/27`, `C_L/C_R = 25/19`.** So `M` is negative at large
  `E` at `c=1/3`; for `c>1/3` the right term eventually dominates positively, for
  `c<1/3` the left dominates negatively.
- The conditional endpoint law:
  ```
  (c - 1/3)log E → (2/27)log(25/19)      and, since E ~ a r^{1-2c},
  (c - 1/3)log r → (2/9)log(25/19) = 0.060985965711502286487...
  ```
  Diagnostics: at `c=0.35` (`m≈188826.674`, `r≈3523.033`) the product is `≈0.136`;
  at the latest log-m point `≈0.07780`. Heavily qualified: *"Applying this
  relation to actual cycles needs a **uniform joint perturbation estimate**…
  Compact-annulus Melnikov theory alone does not supply this estimate when the
  oval escapes. Applying it to the double-cycle condition additionally needs
  differentiated estimates and control of higher-order terms. The observed `r/m`
  decreasing is a useful possible scaling condition, but it has not been proved
  sufficient. It is therefore a conditional asymptotic target for continuation,
  not a certified endpoint law."*
- Explicit non-result: *"No third root, exact K1 exclusion, or completed
  component boundary is established here."*
- On the negative sheet: *"Its inner cycle is unstable and its outer cycle
  stable; both finite antisaddles are attracting at every audited field."*
- `theory_negative_checks.py` performs all of the above data checks with **zero
  ODE calls**; the attempted `c=0.3` corrector failure *"supplies no endpoint
  evidence"*; *"Later file updates are outside the recorded hash snapshot."*

---

## B8. Small items

- `review_2026_09_05/nullcline_exact_check.txt` is only two lines and confirms
  what the summary lists as thread 18: *"PASS: rational section, cubic identity,
  transversality, sensitivities, determinant ratio. No ODE evaluations or
  periodic-orbit certificates were produced."* — i.e. the section theorem's exact
  algebra was replayed, but nothing dynamical.
- Environment/version drift: `q4/requirements.txt` pins
  `numpy==2.3.5, scipy==1.17.0, mpmath==1.3.0` but every verification log records
  the strikes actually running on **`numpy 2.5.2`, `scipy 1.18.1`, `sympy
  1.14.0`, Python 3.14.6**. The pinned file does not match the recorded
  execution environment. `controls/` pins the same stale numpy/scipy plus
  `matplotlib==3.10.8`; `reversible_reseed/` uses loose lower bounds only.
- The Q4 venv path recorded in `q4/data/astra_verification.txt` is
  `/Users/scottg/Documents/Codex/2026-09-04/he/work/q4-venv/bin/python` — a
  **third** execution location, on this Mac, distinct from the two cloud
  sandboxes described in the summary's §7.1.
- Universal resource discipline across all strike replays: one numerical thread,
  lowered priority (`nice +10`), a **ten-CPU-second ceiling per process**. One
  replay actually hit that fuse (B7.4).
- `q4/strike_completion_audit.md` records what strike 1 deliberately left open:
  original-basis endpoint/Wronskian analysis, rigorous quadrature, five-zero
  discovery, and quadratic-field realization — *"They are not used as evidence of
  completion."*
- Strike-1 resolution item 3 records that *"The prior 24-CPU-hour command is
  superseded by the user's bounded reasoning instruction. No production search
  ran."* — i.e. the entire Q4 lane was executed under a bounded-reasoning
  constraint, never as a search.
- Strike-1 resolution item 4: *"Four arbitrary prescribed zeros generically
  annihilate all coefficient vectors. Three anchors determine a direction;
  subsequent zeros require vanishing determinants."*
- Strike-1 resolution item 7: *"The center Taylor series must retain the
  area-period normalization and the factorials when using derivative coefficients
  from the source."*
