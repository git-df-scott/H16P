# LANE BRIEFS (paste one per worker session; each works on its own branch from fable/coordination-2026-09-06)

## LANE 1 — branch fable/lane1-ahcurve — Andronov–Hopf curve sweep
You are Lane 1 of a three-lane research program in the repo H16P (git-df-scott/H16P). Goal: a real planar quadratic vector field with >= 5 limit cycles (refuting H(2)=4). Your lane: the ANDRONOV–HOPF CURVE SWEEP for a fourth limit cycle in ONE nest.
FIRST: `git fetch origin && git checkout fable/coordination-2026-09-06` and read, in order: coordination_2026_09_06/PROTOCOL.md (binding), SEEDS.json, LIT_A_ROTATED_CHERKAS.md sections 1.4, 1.5, 3.1, 3.2, 5.3, 5.4, and H16P_SUMMARY.md sections 0 and 2(a). Then create and work on branch fable/lane1-ahcurve. Do not modify other branches.
MATH: for a base field X=(P,Q) the uniformly rotated family X_b=(P cos b - Q sin b, P sin b + Q cos b) has displacement D(s,b) on a fixed section from the focus that is strictly monotone in b for every s (Duff/Perko). For each s there is a unique beta*(s) closing the orbit. Cycles of X_b are the level set beta*(s)=b. Three cycles <=> two interior extrema; FOUR <=> THREE interior extrema with overlapping height windows. Nobody has searched for a third extremum.
TASKS: (1) Engine: fast correct return map (C + Python driver; audit/fable_engine/retmap.c on branch astra/fastra-afternoon-2026-09-05 may be adapted after reading its REVIEW_engine.md bug list). Section = ray from the focus (Cherkas seeds: y=-1, x>1 through A=(1,-1)). beta*(s) by monotone root solve in b (tol 1e-10). Record s_max where the return fails. Two-tolerance noise estimate (PROTOCOL rule 1). (2) Validation, mandatory: Cherkas rows 1–8 three cycles at listed x to ~1e-2, beta* with exactly 2 interior extrema each; KKL control (3+1); row 4 curve qualitatively matches the published AH polynomial. Write VALIDATION.md. No sweep before validation passes. (3) Sweep: raw 12-coefficient space, focus translated to a fixed point, one scale fixed, rotation integrated out by beta*. From the 9 fat seeds (Cherkas 1–8, Perko P3): random perturbations at relative sizes 1e-3, 1e-2, 1e-1, then hill-climb with primary objective = number of interior extrema of beta*, secondary = min |d beta*/ds| at inflection points OUTSIDE the two-extrema interval, normalized by the height range. 150–300 log-spaced section points per field, local refinement near suspected extrema. Also compute the remote nest's beta* when a second focus exists. (4) Trigger: beta* with >= 3 interior extrema or D with >= 4 sign changes in one nest -> recompute in long double/binary128 or mpmath dps 40, write TRIGGER_lane1_<timestamp>.json with exact rational coefficients, brackets, both-engine values; commit and push at once; do not announce. (5) Commit every 30–45 min; REPORT_lane1.md at branch root; append-only JSONL ledgers with exact coefficient strings and engine hash; RESUME.md before ending. Use all cores. Never claim a counterexample. Never count a cycle from |D|~0. Never spend time on Q4, the reversible reseed, or near-integrable seeds.

## LANE 2 — branch fable/lane2-cusp — cusp manifold continuation to a swallowtail
You are Lane 2. Goal as above. Your lane: CONTINUE THE MANIFOLD OF TRIPLE LIMIT CYCLES (cusps) from the small-amplitude Bautin region to normal amplitude and search along it for a MULTIPLICITY-FOUR cycle (swallowtail). Perko 1995 Thm 4.3 (verbatim in LIT_A_ROTATED_CHERKAS.md §1.6): a multiplicity-four cycle with three nondegeneracy conditions forces an open region nearby with FOUR simple cycles in one nest. Nobody has done this continuation.
FIRST: checkout fable/coordination-2026-09-06; read PROTOCOL.md, SEEDS.json, LIT_A §1.4–1.6 and §3.1 (Cherkas normal form, third-order weak focus family, focal values eq.(15)), perko1992_ProcAMS_bifurcation_geometric_theory.pdf (Lemma 2, Thms 2–3), H16P_SUMMARY.md §0. Work on branch fable/lane2-cusp.
MATH: Cherkas normal form xdot = 1 + x y, ydot = a00 + a10 x + a20 x^2 + a01 y + a11 x y + a y^2, a00 = a01 + a11 - a10 - a20 - a, focus A=(1,-1), section y=-1, x>1, D(x; a,a20,a11,a01,a10). At the third-order weak focus (a11=4-2a, a01=2a+1-a11, a10 as in SEEDS.json, V7!=0): D(r) ~ V1 r + V3 r^3 + V5 r^5 + V7 r^7, and D = V7 r (r^2-r0^2)^3 is a TRIPLE cycle at r0 when V5=-3r0^2 V7, V3=3r0^4 V7, V1=-r0^6 V7; D_rrr there = 48 r0^4 V7 != 0. Cusp manifold {D=D_r=D_rr=0} has dimension 3 in the 5-dim moduli; swallowtail adds D_rrr=0: a codimension-1 event inside the cusp manifold, detectable as a sign change of D_rrr along a path.
TASKS: (1) Engine with D, D_x, D_xx, D_xxx in high precision (mpmath dps 40–50 or binary128 C++: astra_afternoon_2026_09_05/full_return128.cpp, fastra_d1_2026_09_05/matching_quad.cpp on the branches named in H16P_SUMMARY §0). Variational equations to third order if feasible, else centered differences with balanced step; NEVER third derivatives in double. Validate on a Cherkas seed and at a known fold. (2) Enter the cusp manifold at (a,a20) of Cherkas rows 1–8 via the Bautin formulas at small r0, then Newton on D=D_x=D_xx=0 in (a11,a01,a10) at fixed x0=1+r0 (or the better-conditioned parametrization). Confirm a triple root (three near-coincident sign changes under a tiny rotation). (3) Pseudo-arclength continuation in amplitude at fixed (a,a20) until the curve ends (graphic, blow-up, loss of return); record D_xxx and Perko's Jacobians at every point; watch for a SIGN CHANGE of D_xxx. Repeat on an (a,a20) grid. Record endpoints and why. (4) If D_xxx changes sign: Newton on 4 equations in (a11,a01,a10,x0); verify nondegeneracy; unfold into the swallowtail region; count sign changes per PROTOCOL rule 1; four in one nest = TRIGGER (binary128 recheck, TRIGGER_lane2_<timestamp>.json, push, do not announce). (5) Log the beta* picture at every cusp point for Lane 1. (6) Commit every 30–45 min; REPORT_lane2.md (curves found, start/end/why, D_xxx sign, closest approach to zero with exact rationals); RESUME.md before ending. Never claim a counterexample. Never spend time on Q4, reversible reseed, or KKL fold continuation beyond radius 1e10.

## LANE 3 — branch fable/lane3-certify — exact certifier via transversal polygons
You are Lane 3. Goal as above. Your lane: build the EXACT CERTIFIER that turns numerically located normal-size limit cycles into a proof, validated on known fields. The repo specified an interval-ODE (CAPD) certifier three times and never built one; you build a cheaper rigorous alternative.
FIRST: checkout fable/coordination-2026-09-06; read PROTOCOL.md (rule 10 is your spec), SEEDS.json, proving_existence_numerically_detected_planar_limit_cycles_1602.00113.pdf, cherkas2003.pdf, galias_tucker_2022_..._AMC415.pdf §3–4. Work on branch fable/lane3-certify.
MATH: cycles of a quadratic field are convex and enclose exactly one focus; a quadratic field without a center has only isolated periodic orbits. Poincaré–Bendixson: a compact annulus with no equilibrium whose two boundaries are both crossed inward (or both outward) contains a periodic orbit. For each located cycle C_i build two closed polygons with RATIONAL vertices just inside and outside C_i with the flow crossing every edge toward C_i. Edge transversality = X·n along the segment (a univariate quadratic in the edge parameter) has one strict sign on [0,1], decided exactly (endpoints + vertex/discriminant, or Sturm). No equilibrium inside: exact via resultants/rational isolation. Disjoint annuli => distinct cycles. The count becomes a theorem with a machine-checkable certificate.
TASKS: (1) certify.py: Fractions for every proof step; numpy/scipy only to PROPOSE polygons (sample a numerically integrated orbit, offset along the normal, round to rationals, refine failing edges). Output CERTIFICATE_<name>.json + proof summary; independent verify.py re-checking with Fractions only. (2) Validate: a constructed one-cycle field; KKL control (3+1; innermost cycles with multipliers ~1e-3 from 1 may be hard, report which succeed); Cherkas rows 1–8 (should all certify; compare with the paper's exact counts); Perko P3. Report offset widths and polygon sizes. (3) Optional: Cherkas Dulac-function ansatz (paper eq.(6)–(11)) for exact upper counts; validate on row 4 (n=12, k=-1). (4) certify_field(vec12, section_points) entry point for triggers, robust to multipliers within 1e-6 of 1 via thin offsets and mpmath proposals; document limits. (5) Commit every 30–45 min; REPORT_lane3.md with validation table; RESUME.md. Every proof step exact; floating point only for proposals; never claim a count verify.py does not confirm; no interval ODE integrator; no Q4 or fold-surface work.
